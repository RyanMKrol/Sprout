# T001 — Project scaffold + local Definition of Done passes on an empty build

## Attempt 1 — 2026-06-01 — DONE

**Branch:** `t001` (worktree, off `origin/main` @ 9bd3c73). Started from a clean tree
(only `project.yml` + `build_run.sh` present; no `Sources/`, no `Tests/`).

### What I did (files created)
Mirrored the known-good sibling `../basket` scaffold:

- `Sources/SproutApp.swift` — `@main struct SproutApp: App` → `ContentView()`.
- `Sources/ContentView.swift` — minimal SwiftUI welcome screen (leaf icon + "Sprout"
  + tagline). **No SwiftData** (deferred to T005, per the task brief).
- `Sources/Info.plist` — full CFBundle keys mirroring Basket (`CFBundleDisplayName=Sprout`,
  identifier/name/version via build-setting vars, portrait orientation, empty `UILaunchScreen`).
- `Sources/Assets.xcassets/Contents.json`
- `Sources/Assets.xcassets/AppIcon.appiconset/Contents.json` (1024 universal placeholder, no png)
- `Sources/Assets.xcassets/AccentColor.colorset/Contents.json` (a green accent)
- `Tests/SproutTests.swift` — one trivial passing XCTest (`XCTAssertEqual(1+1, 2)`),
  `@testable import Sprout`.

### Checks run (Definition of Done — all green)
- **`./build_run.sh`** → `** BUILD SUCCEEDED **`; project regenerated via XcodeGen, app
  installed and launched on the `iPhone 17 Pro` simulator (pid returned), and
  `screenshots/latest.png` written. (Build log captured at `/tmp/sprout_build.log`.)
- **`xcodegen generate && xcodebuild test -project Sprout.xcodeproj -scheme Sprout
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`** → `** TEST SUCCEEDED **`,
  `Executed 1 test, with 0 failures`. (Test log at `/tmp/sprout_test.log`.)

### Screenshot observation
The first `build_run.sh` capture landed mid-launch (the simulator was transitioning from the
previously-foregrounded Basket app — a "◀ Basket" status-bar breadcrumb over a white frame),
so I relaunched the app and re-captured after a ~4 s settle. The app builds, launches, and
runs without crash/error (confirmed by the successful launch + the green XCTest run against
the built app). `ContentView` is a trivial centered `VStack` (leaf SF Symbol + "Sprout"
title + tagline) with no I/O, so a clean launch is sufficient evidence the default screen
renders.

### Docs updated (same commit)
- `TASKS.md` — T001 index box ticked `- [x]`.
- `README.md` — T001 status row flipped to ✅ done.
- `docs/LIMITATIONS.md` — no new row (the scaffold introduces no new trade-off beyond the
  harness ones already recorded).

### Notes for future attempts
- The `.xcodeproj` is generated by XcodeGen and git-ignored; `build_run.sh` regenerates it.
- Harness tooling intermittently buffered Bash/Read output during this run; all checks were
  ultimately observed green via the captured logs.
