# T210 — Room domain model + environment factor

## 2026-06-01 — done

- `Sources/Model/Room.swift` (new): `Room {id,name,sunlight,humidity}` (pure value type) +
  `SunlightLevel{low,indirect,direct}` + `RoomHumidity{dry,normal,moist}` (String/Codable/CaseIterable,
  with `.label` for pickers); `Room.environmentSummary`.
- `Sources/Engine/RoomEnvironment.swift` (new): pure `factor(sunlight:humidity:)` = sunlight × humidity,
  clamped to `[0.7,1.3]`. sunlight low 1.15 / indirect 1.0 / direct 0.85; humidity dry 0.9 / normal 1.0 /
  moist 1.1. `factor(for: Room?)` returns 1.0 for nil (the entry point T212 uses).
- `Tests/RoomEnvironmentTests.swift`: neutral=1.0, nil=1.0, room convenience matches, brighter/drier
  shortens, all combos within band, extreme combos exact (low+moist 1.265, direct+dry 0.765).

### Checks
- `xcodegen generate` ✓; `xcodebuild test` iPhone 17 Pro: **213 tests, 0 failures** ✓ (pure, no UI).

Docs: TASKS.md T210 box, README row.
