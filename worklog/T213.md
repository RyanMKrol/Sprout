# T213 — Rooms UI + room assignment

## 2026-06-01 — done

- `Sources/ViewModels/RoomsViewModel.swift` (new): `items` (room + plant count), `load`, `add`,
  `update`, `delete`/`delete(atOffsets:)` over the repository room CRUD.
- `Sources/Views/RoomsView.swift` (new): rooms list (name, environment summary, plant count), add/edit
  via a `RoomEditorView` sheet (name + sunlight + humidity segmented pickers), swipe-delete, empty state.
- Room assignment: `BasketAddViewModel.availableRooms`/`loadRooms` + a Room picker in `BasketAddView`
  (assigns the batch → drives roomID + initial cadence); `PlantEditViewModel.selectedRoomID`/`availableRooms`
  + a Room picker in `PlantEditView` (reassigns on save). `Room` made `Hashable` for the picker tag.
- `ContentView.makeRooms`; `PlantListView` gains a temporary Rooms toolbar button + `SPROUT_SCREEN=rooms`
  deep-link (T214 moves room access to the home page). `DemoSeed` seeds Living Room / Bathroom and assigns
  demo plants.
- `Tests/RoomsViewModelTests.swift`: load counts, add, blank-name ignored, delete.

### Checks
- `xcodegen generate` + simulator build → `** BUILD SUCCEEDED **` ✓
- `xcodebuild test` iPhone 17 Pro: **226 tests, 0 failures** ✓
- Screenshot (`-seedDemoData YES`, `SPROUT_SCREEN=rooms`): Rooms list with Bathroom (Low light · Moist,
  1 plant) + Living Room (Direct sun · Dry, 2 plants) + add button. Read `screenshots/latest.png` ✓.

Docs: TASKS.md T213 box, README row.
