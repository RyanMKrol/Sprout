# T202 — Random nickname provider

## 2026-06-01 — done

- `Sources/Model/RandomNicknameProvider.swift` (new):
  - `SeededRandomNumberGenerator` (SplitMix64) — deterministic source for tests + DemoSeed (T204).
  - `RandomNicknameProvider<RNG>` — injectable RNG; `next(avoiding: Set<String>)` returns a name
    not in the taken set (case-insensitive) where possible, else a suffixed fallback ("Alice 2").
    Never loops/crashes; empty pool falls back to "Plant".
  - `EnglishNames.all` — ~125 curated family-friendly English first names (no duplicates).
- `Tests/RandomNicknameProviderTests.swift`: determinism (same seed → same sequence), different
  seeds differ, uniqueness while pool lasts, never returns an avoided name (case-insensitive),
  pool-exhaustion suffix fallback stays unique, empty pool safe, default-pool size/uniqueness.

### Checks
- `xcodegen generate` ✓
- `xcodebuild test` iPhone 17 Pro: **182 tests, 0 failures** ✓ (no UI → no screenshot)

Docs: TASKS.md T202 box ✓, README row ✓, docs/LIMITATIONS.md name-pool row.
