# T203 — Basket add view model

## 2026-06-01 — done

- `Sources/ViewModels/BasketAddViewModel.swift` (new, `@MainActor`):
  - `Entry {id, species, nickname}`; `@Published basket`, `speciesQuery`.
  - `speciesResults` via `careDatabase.search`; `add` (auto unique name, same species repeatable),
    `remove`/`remove(atOffsets:)`, `rename`, `reroll` (yields a different name).
  - auto-naming avoids existing repo nicknames ∪ in-basket names (via `takenNames()`).
  - `canCommit` (non-empty + every species resolves); `commit() -> [Plant]` inserts each via
    `repository.add`, resolves blank names to a fresh random one, returns created plants in basket
    order, clears the basket.
  - `AnyRandomNumberGenerator` (type-erased RNG) so the VM isn't generic.
- `Tests/BasketAddViewModelTests.swift`: picker, auto-name uniqueness, dup species → distinct,
  avoid existing repo names, rename/reroll/remove, canCommit rules, ordered commit, blank→random,
  empty-commit throws.

### Checks
- `xcodegen generate` ✓
- `xcodebuild test` iPhone 17 Pro: **195 tests, 0 failures** ✓ (no UI yet → no screenshot)

Docs: TASKS.md T203 box ✓, README row ✓, docs/LIMITATIONS.md per-row-commit-loop row.
