# T201 — Photo blob on the model

## 2026-06-01 — done

Added an optional plant photo to the model + persistence, plus the encode helper.

- `Sources/Model/Plant.swift`: added `var photoData: Data?` (default `nil`, last init param) —
  `Plant` stays a pure `Codable/Equatable/Sendable` value type (`Data` already conforms; no UIKit).
- `Sources/Persistence/StoredModels.swift`: added `@Attribute(.externalStorage) var photoData: Data?`
  to `StoredPlant`, threaded through `init`, `init(domain:)`, `toDomain()`, and `applyScalars(from:)`.
- `Sources/Model/PlantPhoto.swift` (new): `PlantPhoto.encode(_:maxDimension:jpegQuality:)` — centre-crop
  square → downscale (no upscale) → JPEG. UIKit-aware, kept off the pure `Plant` type.
- Tests: `Tests/PlantPhotoTests.swift` (square output within maxDimension, no upscale, size ceiling,
  JPEG magic bytes, nil for image without CGImage); `Tests/PersistenceTests.swift` extended with photo
  round-trip, no-photo → nil (additive/no-migration guarantee), and update set/clear.

Additive + migration-free: `photoData` is optional and the schema is unversioned, so existing
on-disk records read back `nil` — no `SchemaMigrationPlan` needed.

### Checks
- `xcodegen generate` ✓
- `xcodebuild test` on iPhone 17 Pro: **175 tests, 0 failures** ✓
- No UI (Verify is not simulator-screenshot) — no screenshot required.

Docs in lockstep: `TASKS.md` (Phase 8 section + T201 box ✓ + full specs), `README.md` row ✓,
`docs/LIMITATIONS.md` photo-blob row.

### SourceKit note
The editor showed "No such module UIKit/XCTest" and "Cannot find type CheckIn/Plant" while editing —
SDK-context indexing noise; the real iOS simulator build/test is green.
