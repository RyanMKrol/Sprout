# T206 — Photo-capture coordinator

## 2026-06-01 — done

- `Sources/ViewModels/PhotoCaptureCoordinator.swift` (new, `@MainActor`):
  - `Target {id, nickname, species}` with `init(plant:)` (built from the basket commit order).
  - `index`, `isFinished`, `current`, `cameraAvailable`, `bannerText`
    ("Now photographing <nickname> — <species>"), `progressText` ("2 of 5").
  - `captureCurrent()`: `camera.capture()` → `PlantPhoto.encode` → load plant → set `photoData`
    → `update` → advance. A failed/empty capture stays put (retry), distinct from `skip()`.
  - `skip()` advances without saving; `advance()` sets `isFinished` at the end; empty targets
    finish immediately.
- `Tests/PhotoCaptureCoordinatorTests.swift` (stub camera + in-memory repo): starts on first,
  banner/progress text, capture saves+advances, skip advances without saving, full walk finishes
  with the right photos set, failed capture stays+doesn't save, camera-availability reflects the
  source, empty targets finish immediately.

### Note
The stub is `@MainActor`-isolated (it conforms to the `@MainActor PhotoCapturing`), so it can't be
a default-argument value (nonisolated context) — constructed inside the `@MainActor` test helper.

### Checks
- `xcodegen generate` ✓; `xcodebuild test` iPhone 17 Pro: **206 tests, 0 failures** ✓ (logic only → no screenshot)

Docs: TASKS.md T206 box ✓, README row ✓.
