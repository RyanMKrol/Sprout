# T003 — Domain model types (Plant, CareProfile, CheckIn) — pure Swift

## 2026-06-01 — attempt 1 (done)

**Starting state:** clean tree on `t003`, no prior partial work. `Sources/` held only the
T001/T002 scaffold (`SproutApp`, `ContentView`, `DemoSeed`); no `Sources/Model/` yet.

**What I did** (per `docs/designs/adaptive-watering.md`):
- Added `Sources/Model/CareProfile.swift` — `MoisturePreference` enum
  (`driesOut`/`evenlyMoist`/`staysMoist`, raw values = the `care_database.json` contract for
  T004) and the `CareProfile` value type (`species`, `baseIntervalDays`, `minIntervalDays`,
  `maxIntervalDays`, `moisture`). Exposes `isValid` (the single-record invariant
  `min ≤ base ≤ max`, positive, non-blank species) that T004's dataset validator builds on.
  `id == species` (the unique key).
- Added `Sources/Model/CheckIn.swift` — `SoilMoisture` (`dry`/`moist`/`wet`),
  `LeafState` (`fine`/`droopy`), and the `CheckIn` value type (`id`, `date`, `soil`, `leaves`,
  `watered`).
- Added `Sources/Model/Plant.swift` — the owned-plant value type holding identity + the
  learned scheduling state (`adj` default `1.0`, `lastWatered`, `nextDue`, `checkIns`). Named
  constants `Plant.adjRange = 0.5...2.0` and `Plant.defaultAdj = 1.0` per the design's clamp.
- All types are `Codable`, `Equatable`, `Identifiable`, `Sendable`. **No SwiftUI / SwiftData /
  UIKit imports** — only `Foundation` (for `Date`/`UUID`).
- Added `Tests/ModelTests.swift` — 16 tests covering construction, enum raw-value contract,
  `Codable` round-trips, `Equatable`, decoding from database-shaped JSON, and the `isValid`
  invariant (ordered / equal / base-below-min / base-above-max / non-positive / blank-species).

**Checks run:**
- `xcodegen generate` — OK.
- `xcodebuild test … -scheme Sprout -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
  → **TEST SUCCEEDED**, 17 tests, 0 failures (16 new ModelTests + the existing trivial test).
- `./build_run.sh` → **BUILD SUCCEEDED**, screenshot written. (T003 has no
  `Verify: simulator-screenshot` — it's a pure-model task with no UI change — so the screenshot
  is only the build-clean DoD check, not an empirical observation.)

**Docs in lockstep (same commit):** TASKS.md T003 box `- [x]`; README.md T003 row → ✅ done;
docs/LIMITATIONS.md gains a row on the construction-time-vs-`isValid` validation trade-off.

**What remains:** nothing for T003. T004 (care DB loader/validator) and T005 (SwiftData
persistence) depend on these types and are next in dependency order.
