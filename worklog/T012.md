# T012 — "Why this schedule" explanation text

## 2026-06-01

**Resumed** an interrupted attempt that had already produced the full implementation as
uncommitted/untracked work. Reconciled the delta against the task's `Done-when:` — found the
work essentially complete and verified it end to end rather than redoing it.

### What was present (and verified real)
- `Sources/Engine/ScheduleExplanation.swift` — pure `ScheduleExplanation` value (direction +
  cause + day counts) and `ScheduleExplanationBuilder`. Builds the sentence
  (*"Every 4 days — shortened from 6 because it dried out faster than expected."*) and a
  compact `pillSummary` (*"Every 4d · shortened"*) from schedule inputs, reusing
  `ScheduleEngine` for the effective-interval rounding/clamping.
- `Tests/ScheduleExplanationTests.swift` — 12 cases: direction (shortened/lengthened/
  unchanged), every check-in cause, settled-vs-starting, weather-hook causes (T016-ready),
  check-in-beats-weather precedence, max-clamp interaction, singular day wording.
- View wiring: `PlantDetailViewModel.explanation` (shown on detail, falls back to T008
  `scheduleSummary` placeholder when no care record); `PlantListViewModel` now injects the
  care DB (optional, defaults `nil`) and exposes `whySummary` per card; `PlantDetailView`,
  `PlantListView`, `ContentView`, `DemoSeed` updated. Lily seeded with `adj 0.7` so the detail
  screenshot shows a real "shortened" sentence.

### Checks run (local Definition of Done — all green)
1. `./build_run.sh "iPhone 17 Pro" -seedDemoData YES` → `** BUILD SUCCEEDED **`.
2. `xcodebuild test … -scheme Sprout -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
   → **132 tests, 0 failures** (incl. the 12 `ScheduleExplanationTests`).
3. **Screenshot (empirical `Verify:`):** first list capture came out blank (the known
   mid-launch/stale-binary issue in CLAUDE.md/LIMITATIONS). Re-launched with
   `SPROUT_SCREEN=detail`, waited ~8 s with the Simulator window open, re-captured
   `screenshots/latest.png` → Lily's detail screen shows the Schedule section reading
   **"Every 4 days — shortened from 6 because it dried out faster than expected."** over the
   check-in history. Matches the T012 intent exactly.

### Docs in lockstep (this commit)
- `TASKS.md` T012 index box ticked `[x]`.
- `README.md` T012 status row → ✅ done.
- `docs/LIMITATIONS.md` — added two rows: single-dominant-cause (no blended explanation;
  attribution inferred from latest check-in, not the persisted nudge) and the out-of-scope
  view-model/`DemoSeed` wiring.

### Remaining
Nothing — task complete, local DoD green, ready to integrate.
