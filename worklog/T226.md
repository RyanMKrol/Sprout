# T226 — Care DB gap-fill batch 2 (~15 more → ~335)

## 2026-06-02 — attempt 1 (done)

**Task:** Add the next ~15 species (Tier 2 from the T224 gap list) to `care_database.json`,
same sourcing + provenance rules as T225, carrying the dataset past ~320. Scope:
`Sources/Resources/care_database.json`, `docs/research/uk-houseplants.md`.

**What I did:**
- Resumed on a clean `t226` branch (T225 already shipped, dataset at **320**). Read the T224
  deliverable (`docs/research/care-db-audit.md` §3 **Tier 2**, rows 16–30) and used its 15
  proposed rows verbatim — display name, moisture, base/min–max already validator-checked there.
- Confirmed none of the 15 Tier-2 names (trim+lowercase normalised, per
  `CareDatabaseValidator.normalisedKey`) already existed in the shipped file.
- Appended the 15 records to `care_database.json` (indent-2 JSON, matching existing format) via
  a Python pass that re-asserted `min ≤ base ≤ max`, valid moisture, and no normalised-name
  duplicate across the whole file → **335** species, dup-free (105-line insertion, no reformat
  of existing rows).
- Added 15 Provenance rows to `uk-houseplants.md` (one per plant, T226-tagged, source/anchor
  rationale matching the existing column format). Most are genus-anchor grounded (calatheas,
  peperomias, philodendron/alocasia/aglaonema cultivars, hoya, anthurium, tradescantia, pilea,
  ceropegia, sansevieria), as flagged in the audit.
- Docs in lockstep: TASKS.md T226 box ticked, README.md T226 row → ✅ (335), LIMITATIONS.md
  T224 care-DB row extended with a T226 confirmation (dataset now 335; Tier 2 = secondary
  cultivars deliberately past the ~320 core target).

**Checks run:**
- `xcodegen generate` + `xcodebuild test … -scheme Sprout -destination 'platform=iOS Simulator,
  name=iPhone 17 Pro' -only-testing:SproutTests/CareDatabaseTests` → **11 tests, 0 failures**;
  `testShippedDatabaseDecodesAndValidates` green against the new 335-species file.
- Full SproutTests suite → **TEST SUCCEEDED**.

**Remains:** nothing for T226. T227 is the final reconciling audit (target ≥315 unique;
dataset now stands at 335).
