# T200 — Compile key-decision review packet for human sign-off

## 2026-06-01 — built, verified, pushed

**What I did.** Wrote `docs/REVIEW.md`, the final sign-off packet. Compiled every key judgement
call the backlog baked in, each with a `file:line` pointer and the knob to change it:

1. The schedule formula (`ScheduleEngine.effectiveInterval`/`nextDue`).
2. The adaptive engine — `recheckDays` (3), `timingBand` (0.20), the `adj` clamp
   (`Plant.adjRange` 0.5–2.0), and the full 9-row nudge-factor decision table from
   `AdaptiveEngine.decide`.
3. Environment→factor mapping. **Noted the history**: live weather (T015/T016) was retired by
   **T212** in favour of `RoomEnvironment` (the live path now); documented the dormant
   `WeatherFactor` constants too so re-enabling is possible.
4. Notification defaults — default hour 09:00, user-configurable 0–23.
5. Check-in UX (3 enum inputs, didWater semantics) + verification approach (screenshot + XCTest,
   no XCUITest/SwiftLint) and its trade-off.
6. Care DB (305 species) — pointer to the JSON + the 1:1 provenance index.

Plus an explicit **"Low-confidence / please check"** section: the 176/305 genus-anchor rows
(esp. heavy-variegation cultivars bumped on a chlorophyll heuristic, jewel Alocasias, carnivorous
plants), the engine constants I'd most want a second opinion on, and the structural limitations.

Docs in lockstep (same commit): ticked `TASKS.md` T200 box, flipped the `README.md` status row to
✅ done with a link, and de-parenthesised the existing `docs/REVIEW.md` mention into a proper link.
No new `LIMITATIONS.md` row — this task documents existing limitations, it introduces none.

**Checks run (full local Definition of Done — all green):**
- `./build_run.sh "iPhone 17 Pro"` → `** BUILD SUCCEEDED **`, app launched, screenshot written.
- `xcodegen generate && xcodebuild test … -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
  → `** TEST SUCCEEDED **`, 233 tests, 0 failures.
- Cross-checked every value cited in REVIEW.md against source (engine constants, scheduler hour,
  RoomEnvironment table, provenance rows for the named cultivars).

**What remains:** nothing. This is the final backlog task — when it merges, the core backlog is
complete and ready for end-to-end human review.
