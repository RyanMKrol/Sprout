# Worklog — T004 Care database loader, schema & validator

## 2026-06-01 — built (first attempt)

**Starting state:** clean `t004` branch off `main` at `348c298` (T003 merged). No prior partial
work. T003 domain types (`CareProfile`, `MoisturePreference`, etc.) confirmed present and merged.

**What I did (the pipeline, not the data — T101–T130 add the ~300 plants):**
- `Sources/Resources/care_database.json` — a small **valid seed** of 5 example plants (Pothos,
  Snake Plant, Monstera deliciosa, Boston Fern, Peace Lily), a top-level array of `CareProfile`-
  shaped records matching the `moisture` enum raw values.
- `Sources/Model/CareDatabase.swift`:
  - `CareDatabaseError` — structured errors (`invalidRecord`, `duplicateSpecies`, `resourceMissing`).
  - `CareDatabaseValidator.validate(_:)` — the **reusable** dataset-level validator (used here and
    by every T101–T130 batch): each record `isValid` (`min ≤ base ≤ max`, positive, non-blank) and
    species unique, case-/whitespace-normalised.
  - `CareDatabase` — `decode`/`load`(from data) and `loadBundled(from:resource:)`; sorts by species;
    `search(_:)` (case- + diacritic-insensitive substring; empty → full list) and
    `profile(forSpecies:)` for the picker (T007).
- `Tests/CareDatabaseTests.swift` — 11 tests: valid file decodes + sorts; fields/enum preserved;
  malformed record rejected; duplicate species rejected; clean dataset accepted; missing resource
  throws; search case/diacritic-insensitive; empty/no-match search; and the **real shipped JSON**
  decodes + validates (loaded via `#filePath`-relative path).

**Checks run (local Definition of Done — all green):**
- `xcodebuild test … -scheme Sprout -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` →
  **TEST SUCCEEDED**, 28 tests, 0 failures (11 new CareDatabase tests).
- `./build_run.sh` → `** BUILD SUCCEEDED **` + screenshot written.
- Confirmed `care_database.json` is copied into `Sprout.app` (bundled for runtime use downstream).

**Docs in lockstep (same commit):** TASKS.md box `- [x]`, README.md row → ✅, LIMITATIONS.md row
(unversioned array + display-name key + `#filePath`-coupled shipped-file test).

**What remains:** nothing for T004 — the ~300 plants are added by T101–T130; final count/dedup at T131.
