# T204 — Basket add view + "+" rewiring 🚦

## 2026-06-01 — done

- `Sources/Views/BasketAddView.swift` (new): `List` with a basket section (inline-editable
  nickname `TextField` + shuffle/reroll button + swipe-to-delete) and a species-search section
  (full care-DB list, green add buttons). Toolbar Cancel → `onFinish(.cancelled)`; "Add N Plants"
  (disabled unless `canCommit`) → `commit()` → `onFinish(.created(plants))`. `BasketAddResult` enum.
- `Sources/Views/PlantListView.swift`: added `makeBasket` factory param; the "+" toolbar button now
  presents the basket (`.sheet(isPresented:)`) instead of `editorMode = .add`. Edit-swipe still uses
  `makeEditor`/`PlantEditView`. Deep-link `SPROUT_SCREEN=add`/`basket` → present the basket.
- `Sources/ContentView.swift`: `makeBasket()` against the shared repo/care DB; under `DemoSeed.isActive`
  uses a fixed RNG seed + pre-fills `DemoSeed.basketSampleSpecies` for a stable, populated screenshot.
- `Sources/DemoSeed.swift`: `basketSampleSpecies = [Pothos, Snake Plant, Monstera deliciosa]` (DEBUG-only).

### Checks
- `xcodegen generate` + simulator build → `** BUILD SUCCEEDED **` ✓
- `xcodebuild test` iPhone 17 Pro: **195 tests, 0 failures** ✓
- Screenshot (`-seedDemoData YES`, `SPROUT_SCREEN=basket`): basket shows "Add Plants",
  "Add 3 Plants", Basket(3) = Violet/Pothos, Charlotte/Snake Plant, Ava/Monstera with reroll
  controls, and the searchable species list with add buttons. Read `screenshots/latest.png` ✓.

### Gate
🚦 This changes the primary add UX (the "+" now opens the basket, not the single-plant form). The
single-plant form remains for the edit-swipe path. Surfaced to the user with the screenshot before
continuing to the camera tasks (T205+).

Docs: TASKS.md T204 box ✓, README row ✓ (no new LIMITATIONS row — covered by T203's commit-loop row).
