# T207 — Camera overlay view + AVFoundation capture 🔒 (device path)

## 2026-06-01 — done (screenshot path); device capture pending human verification

- `Sources/Camera/AVFoundationCamera.swift` (new, the only AVFoundation importer):
  `AVCaptureSession` (.photo) + back-camera input + `AVCapturePhotoOutput`; `capture()` bridges the
  photo-output delegate to async via a continuation; `isAvailable` reflects auth + device. Conforms
  to `CameraPreviewProviding` (`makePreview()`/`start()`/`stop()`) and embeds `CameraPreviewView`
  (`UIViewRepresentable` over `AVCaptureVideoPreviewLayer`). `@preconcurrency import AVFoundation`
  silences the non-Sendable session warnings.
- `Sources/Views/PhotoCaptureView.swift` (new): black full-screen; banner (nickname / species /
  "N of M"); square preview (live when `cameraAvailable`, else a "camera unavailable" placeholder);
  shutter (`captureCurrent`) + Skip; `isFinished` → `onFinish` (stops the session).
- `Sources/ContentView.swift`: `makeCamera()` (stub on simulator/demo, `AVFoundationCamera` on device)
  + `makePhotoCapture(targets:)`; threaded into `PlantListView`.
- `Sources/Views/PlantListView.swift`: `makePhotoCapture` param, `.fullScreenCover`, and a
  `SPROUT_SCREEN=camera` deep-link (builds targets from the seeded list items).
- `Sources/ViewModels/PhotoCaptureCoordinator.swift`: exposed `camera` (was private) for the preview.

### Checks
- `xcodegen generate` + simulator build → `** BUILD SUCCEEDED **` (no warnings after `@preconcurrency`) ✓
- `xcodebuild test` iPhone 17 Pro: **206 tests, 0 failures** ✓
- Screenshot (`-seedDemoData YES`, `SPROUT_SCREEN=camera`): banner "Lily / Peace Lily / 1 of 5",
  square preview placeholder, centred shutter, Skip. Read `screenshots/latest.png` ✓.

### 🔒 Needs-human (device)
The real `AVFoundationCamera` path — session, live preview, permission prompt, captured photo bytes —
**cannot run on the simulator** and is unverified by the automated gate. Verify on the iPhone (signing
is set up): run the basket flow → take photos → confirm each photo persists to its plant and the
sequence advances. Recorded as the device check; not auto-completed.

Docs: TASKS.md T207 box ✓, README row ✓, docs/LIMITATIONS.md camera-not-on-sim + presentation-wiring rows.
