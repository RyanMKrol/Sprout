# T227 — Care DB final audit (~320 unique, deduped, validated)

## 2026-06-02 — attempt 1 (done)

**Task:** Verify the assembled care dataset end to end (after T225 Tier 1 + T226 Tier 2):
~320 unique species, no normalised-name duplicates, every record valid, Provenance index +
audit doc reconciled against the JSON, the previously-missing common plants (incl. *Monstera
adansonii*) present, gap list closed. Scope: `care_database.json`, `uk-houseplants.md`,
`care-db-audit.md`, `Tests/CareDatabase*`.

**What I did — this is a reconciling audit, the data already shipped (335) under T225/T226:**
Ran a full end-to-end reconciliation in Python against the live files and recorded the result
in the docs. No JSON edits were needed — the dataset was already complete and clean.

**Audit results (all pass):**
- **335 unique** species (`Done-when` floor is ≥ 315; past the ~320 core target). Moisture
  split `evenlyMoist` 192 · `driesOut` 91 · `staysMoist` 52.
- **0 normalised-name duplicates** (trim + lowercase key, matching `CareDatabaseValidator`).
- **Every record valid:** all 335 satisfy `min ≤ base ≤ max` and a valid `moisture` enum.
- **Provenance 1:1:** the index in `uk-houseplants.md` holds exactly **335 rows** — 0 JSON
  species without a row, 0 rows without a JSON species. Both the *Monstera adansonii* alias
  and its "Swiss Cheese Vine" twin carry rows.
- **Gap list closed:** all **30** Tier-1 (15) + Tier-2 (15) rows from `care-db-audit.md` §3
  are present by display name, including the required *Monstera adansonii* (#1, shipped as a
  display-name alias of "Swiss Cheese Vine" with identical care numbers). No remaining gaps.

**Docs in lockstep:**
- `care-db-audit.md` — added **§5 Final reconciliation (T227)** recording the closed gap list,
  the 335/335 Provenance match, and the justified residual (anchor-grounded cultivar intervals).
- `LIMITATIONS.md` — extended the T224 care-DB row with a **T227 final-audit** update.
- `TASKS.md` T227 box ticked; `README.md` T227 row → ✅ (335 unique, deduped, 1:1 provenance).

**Checks run:**
- `xcodegen generate` → clean.
- `xcodebuild test … -only-testing:SproutTests/CareDatabaseTests` → **11 tests, 0 failures**
  (`testShippedDatabaseDecodesAndValidates` loads + validates the whole 335-species file green).
- Full `SproutTests` suite → **TEST SUCCEEDED**.

**Remains:** nothing. Final audit complete; dataset validated, deduped, fully reconciled.
