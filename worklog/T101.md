# Worklog — T101 Care DB batch 01 — Pothos & Epipremnum cultivars

## 2026-06-01 — built (first attempt)

**Starting state:** clean `t101` worktree off `main` at `84006d2` (T016 merged). No prior partial
work. Prereq T004 (loader + reusable `CareDatabaseValidator`) confirmed merged; shipped
`care_database.json` held the 5-plant seed (Pothos, Snake Plant, Monstera deliciosa, Boston Fern,
Peace Lily).

**Category:** the TASKS.md T101 header shows an empty `()`, but the authoritative batch plan in
`docs/research/uk-houseplants.md` (Design field) assigns **batch 01 = Pothos & Epipremnum cultivars**.
Followed the design doc.

**What I did:**
- Added **10 new unique species** to `Sources/Resources/care_database.json` (5 → 15 records), all
  aroids → `evenlyMoist`:
  Golden Pothos (8/6–14), Marble Queen Pothos (9/6–14), Neon Pothos (8/5–12), Jade Pothos (8/6–12),
  Pearls and Jade Pothos (9/6–14), Manjula Pothos (9/6–14), Snow Queen Pothos (10/7–14),
  Cebu Blue Pothos (8/6–12), Satin Pothos / *Scindapsus pictus* 'Argyraeus' (10/7–16),
  Silver Satin Pothos / *S. pictus* 'Exotica' (10/7–16).
- None collide with the existing generic "Pothos" seed (distinct display names).
- Grounding: web search confirmed *Epipremnum* "every 1–2 weeks, let top 2\" dry" (The Sill /
  Happy Houseplants) and *Scindapsus* "more tolerant of under-watering" → longer interval
  (Smart Garden Guide / Leafy Place). Variegated cultivars nudged slightly longer from the aroid
  genus anchor (less chlorophyll → slower growth). Each rationale recorded.
- Appended 10 rows to the Provenance index and ticked batch 01 (✅) in the batch-plan table.

**Checks run (local Definition of Done — all green):**
- JSON sanity (python): 15 records, 0 duplicate species, all `min ≤ base ≤ max` + valid `moisture`.
- `xcodegen generate` OK; `xcodebuild test … -scheme Sprout -destination 'iPhone 17 Pro'` →
  **TEST SUCCEEDED**, 167 tests, 0 failures (includes T004's real-shipped-JSON decode+validate test,
  which now covers the 15-record file).
- No `Verify: simulator-screenshot` on this task (data-only, no UI change) — no screenshot required.

**Docs in lockstep (same commit):** TASKS.md box `- [x] T101`; README.md row → 🔨 in progress (1/30);
`docs/research/uk-houseplants.md` Provenance index + batch-plan tick; `docs/LIMITATIONS.md` row on
genus-anchor cultivar grounding.

**What remains:** nothing for T101. Batches 02–30 (T102–T130) continue the build-out; T131 dedups/reviews.
