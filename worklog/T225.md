# T225 — Care DB gap-fill batch 1 (~15 missing common species)

## 2026-06-02 — attempt 1 (done)

**Task:** Add the top ~15 missing common species from the T224 gap list (Tier 1, incl.
*Monstera adansonii*) to `care_database.json`, each validator-clean with a Provenance row,
taking the dataset to ~320. Scope: `Sources/Resources/care_database.json`,
`docs/research/uk-houseplants.md`.

**What I did:**
- Read the T224 deliverable (`docs/research/care-db-audit.md` §3 Tier 1) and used its 15
  proposed rows verbatim — display name, moisture, base/min–max already validator-checked there.
- Confirmed the shipped dataset was **305** species and that none of the 15 Tier-1 names
  (normalised) already existed; confirmed the *Monstera adansonii* profile already ships as
  "Swiss Cheese Vine" (8 / 5–12), so adansonii is a deliberate display-name **alias** with the
  same care numbers (Pothos / Golden Pothos precedent), not a new lookup.
- Appended the 15 records to `care_database.json` (indent-2 JSON, matching existing format),
  re-asserting `min ≤ base ≤ max`, valid moisture, and no normalised-name duplicate across the
  whole file in a Python check → **320** species, dup-free.
- Added 15 Provenance rows to `uk-houseplants.md` (one per plant, T225-tagged, source/anchor
  rationale matching the existing column format).
- Docs in lockstep: TASKS.md T225 box ticked, README.md T225 row → ✅ (320), LIMITATIONS.md
  T224 care-DB row updated with a T225 confirmation (dataset now 320; several cultivars on the
  genus anchor).

**Checks run:**
- `xcodegen generate` + `xcodebuild test … -scheme Sprout -destination 'platform=iOS Simulator,
  name=iPhone 17 Pro' -only-testing:SproutTests/CareDatabaseTests` → **11 tests, 0 failures**;
  `testShippedDatabaseDecodesAndValidates` green against the new 320-species file.
- Full SproutTests suite run (see final entry) — all green.

**Remains:** nothing for T225. T226 takes Tier 2 (→ ~335); T227 is the final audit.
