# T205 — Camera seam + stub + permission

## 2026-06-01 — done

- `Sources/Camera/PhotoCapturing.swift` (new): `@MainActor protocol PhotoCapturing` —
  `isAvailable: Bool`, `capture() async -> UIImage?`. The single seam between the photo flow and
  the camera hardware; downstream never imports AVFoundation directly.
- `Sources/Camera/StubPhotoCapturing.swift` (new): reports `isAvailable=false` (sim) but returns a
  generated green/leaf square from `capture()`, so the flow is screenshottable + testable without
  hardware. `returnsImage:false` variant exercises the failure branch.
- `project.yml`: added `NSCameraUsageDescription` (mirrors the location key). Verified present in
  the generated `Sources/Info.plist`.
- `Tests/StubPhotoCapturingTests.swift`: availability flag; capture returns a `PlantPhoto.encode`-able
  image; failure variant returns nil.

### Note on scope
`ContentView.makeCamera()` was deferred to T207 (it must choose the real `AVFoundationCamera` on
device, which doesn't exist until T207) — adding it here would be a forward reference. The coordinator
(T206) takes an injected `PhotoCapturing`, so nothing needs `makeCamera` until the view lands.

### Checks
- `xcodegen generate` ✓; `xcodebuild test` iPhone 17 Pro: **198 tests, 0 failures** ✓ (no UI → no screenshot)

Docs: TASKS.md T205 box ✓, README row ✓.
