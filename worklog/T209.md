# T209 — Balanced ~300-name pool

## 2026-06-01 — done

Scan of the old pool: 52 girls' / 46 boys' / 14 unisex = 112 (unisex skewed floral, so it *felt*
female-heavy).

- `Sources/Model/RandomNicknameProvider.swift`: split `EnglishNames` into testable `girls` (148),
  `boys` (140), `unisex` (20) sub-lists; `all = girls + boys + unisex` = **308 unique**, balanced.
- `Tests/RandomNicknameProviderTests.swift`: pool ≥300 + union check; new gender-balance test
  (girls vs boys within 10%).
- `docs/LIMITATIONS.md`: name-pool row updated (~300, gender-split).

### Checks
- `xcodegen generate` ✓; `xcodebuild test` iPhone 17 Pro: **207 tests, 0 failures** ✓ (no UI).
- Verified no duplicates across the three lists (308 unique).

Docs: TASKS.md Phase 9 index + T209 box + specs, README rows.
