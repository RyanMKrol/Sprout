# T118 — Care DB batch 18 — Begonia

## 2026-06-01

**Category resolution.** The TASKS.md *detail block* (line 447) is mislabelled "Peperomia"
(that was T117, already merged). The canonical **index** (line 126) and the batch-plan table in
`docs/research/uk-houseplants.md` (row 18) both say **Begonia** — followed that. No Begonia was
present in the dataset before this batch.

**What I did.** Added **10 new unique Begonia species/cultivars** to
`Sources/Resources/care_database.json` (175 → 185 records), all `evenlyMoist` per the genus
anchor (base 6 / 4–9), with per-type deviations:
- Rex/rhizomatous (Rex, Iron Cross, Escargot) — anchor cadence, water from below.
- Cane / angel-wing (Polka Dot, Angel Wing, Dragon Wing, Palm-leaf) — fleshy water-storing
  stems → slightly longer interval (7 / 5–10).
- Beefsteak (× erythrophylla) — thick rounded leaves, more drought-tolerant → 7 / 5–11.
- Wax (semperflorens) & Tuberous — keep evenly moist in growth, shorter interval → 5 / 4–8.

Appended one Provenance-index row per plant in `docs/research/uk-houseplants.md` (RHS / Hortology
where reachable, else genus-anchor rationale) and ticked batch-plan row 18 ✅.

**Checks run.**
- `python3` validation: 185 records, all scientific/common names unique (normalised), every row
  `min ≤ base ≤ max`, valid `moisture`.
- `xcodegen generate` → OK.
- `xcodebuild test … -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` →
  **167 tests, 0 failures** (incl. `testShippedDatabaseDecodesAndValidates`).

No screenshot Verify — data-only change, no UI surface affected.

**Docs in lockstep.** TASKS.md index box ticked, README status row → 18/30, provenance index +10
rows, batch-plan row 18 ✅. No new limitation.

**Remaining:** none. Ready to integrate.
