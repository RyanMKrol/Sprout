# T211 — Room persistence + plant→room link

## 2026-06-01 — done

- `Sources/Model/Plant.swift`: `roomID: UUID?` (additive, default nil, last init param).
- `Sources/Persistence/StoredModels.swift`: `StoredPlant.roomID` threaded through all mapping fns;
  new `StoredRoom @Model` (id/name/sunlight/humidity, Codable enums like the check-in precedent) +
  `init(domain:)`/`toDomain()`/`applyScalars`.
- `Sources/Persistence/PlantRepository.swift`: room CRUD — `allRooms/room(id:)/addRoom/updateRoom/deleteRoom`.
- `Sources/Persistence/SwiftDataPlantRepository.swift`: implemented; `deleteRoom` nils assigned plants'
  `roomID` (plants survive); `schema = [StoredPlant, StoredCheckIn, StoredRoom]`.
- `Sources/ContentView.swift`: `EmptyPlantRepository` gains the room no-ops.
- `Tests/PersistenceTests.swift`: room round-trip, sort, update, update-missing throws, plant.roomID
  round-trip (set + nil), delete-room nils plants but keeps them, delete-missing throws.

Migration-free: `roomID` optional + `StoredRoom` additive → existing records unaffected.

### Checks
- `xcodegen generate` ✓; `xcodebuild test` iPhone 17 Pro: **221 tests, 0 failures** ✓.

Docs: TASKS.md T211 box, README row, docs/LIMITATIONS.md room-link row.
