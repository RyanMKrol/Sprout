# T214 — Tile home page + show photos

## 2026-06-01 — done

- `Sources/Views/HomeView.swift` (new): owns the app `NavigationStack`; three large `HomeTile`s —
  My Plants (count), Rooms (Light & humidity), "Water your plants" (due count) — + a Settings gear.
  Tiles push `.plants`/`.rooms` destinations; deep-link routing for home/plants/rooms/settings/etc.
- **De-nested `PlantListView`**: removed its own `NavigationStack`/`path` + the settings/rooms toolbar
  (now on Home); kept the list, the `+` add button, the `UUID` plant-detail destination, and its sheets
  (editor/basket/photo/prompt). Trimmed its deep-link to the sheet screens (add/basket/camera/photoprompt).
- **Photos shown**: `PlantThumbnail` (photo or tinted leaf placeholder); `PlantListViewModel.Item` carries
  `photoData` and the card shows the thumbnail; `PlantDetailViewModel.photoData` + a header photo in
  `PlantDetailView`.
- `ContentView` now renders `HomeView` (passing all factories + `makeRooms`/`makeSettings`).

### Interim note
The "Water your plants" tile currently pushes the plants list; **T215 replaces it with the guided
watering flow**.

### Checks
- `xcodegen generate` + simulator build → `** BUILD SUCCEEDED **` ✓
- `xcodebuild test` iPhone 17 Pro: **226 tests, 0 failures** ✓
- Screenshots: `SPROUT_SCREEN=home` shows the three tiles with live counts (5 plants / 2 due);
  `SPROUT_SCREEN=plants` confirms the de-nested list still pushes from home (back chevron), with
  leaf-placeholder thumbnails + room-driven "why" summaries. Read `screenshots/latest.png` ✓.

Docs: TASKS.md T214 box, README row.
