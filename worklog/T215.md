# T215 — Guided watering walkthrough

## 2026-06-01 — done

- `Sources/ViewModels/GuidedWateringCoordinator.swift` (new, `@MainActor`): walks an ordered
  `[Plant]`. `soil`/`leaves` inputs; `preview(now:)` runs `AdaptiveEngine.update` (watered=false) to
  show a water/skip recommendation **without persisting**; `confirm(watered:now:)` records the real
  `CheckIn` + recomputes the schedule (same path as `CheckInViewModel.submit`) and advances; `skip()`
  advances; inputs reset between plants; `isFinished`. `message`/`recommendsWater`/`progressText` for the view.
- `Sources/Views/GuidedWateringView.swift` (new): per plant — photo + name + progress, "How does it
  look?" (leaves) + "How's the soil?" (soil) → "Check" → recommendation + "I watered it / Didn't water"
  (or "Next plant"); Close + Skip; completion / nothing-to-water states.
- Wiring: the home **Water** tile now opens a mode chooser — *Plants due now (N)* or *Full check-in (N)* —
  presenting `GuidedWateringView` (`.fullScreenCover`). `ContentView.makeGuidedWatering(mode:)` builds the
  due/all plant set (due-order) with the room environment factor. `SPROUT_SCREEN=water` deep-links it.
- `Tests/GuidedWateringCoordinatorTests.swift`: starts on first; preview doesn't persist; confirm(watered)
  persists + recomputes + advances; skip advances without persisting; full walk finishes; empty set
  finishes; inputs reset between plants.

### Checks
- `xcodegen generate` + `xcodebuild test` iPhone 17 Pro: **233 tests, 0 failures** ✓
- Screenshot (`-seedDemoData YES`, `SPROUT_SCREEN=water`): the guided step for Lily (1 of 5) — photo,
  leaves + soil segmented controls, Check button, Close/Skip. Read `screenshots/latest.png` ✓.

Docs: TASKS.md T215 box, README row.
