# T131 — Care database complete, deduped & reviewed

## 2026-06-01

End-to-end audit of the assembled care dataset.

### What I did
- Reconciled `Sources/Resources/care_database.json` against the Provenance index in
  `docs/research/uk-houseplants.md` with a script:
  - JSON holds **305 unique** display-`species` records (≥ 290 required ✓), no duplicate species.
  - Every record valid: `min ≤ base ≤ max` and a valid `moisture` enum value.
  - Provenance index had **300** rows — the 5 original **seed plants** (Pothos, Snake Plant,
    Monstera deliciosa, Boston Fern, Peace Lily) predate the batch-append convention and had
    no rows. **Filled the gap**: appended one Provenance row each (scientific name, moisture,
    base/min–max, RHS/The Sill rationale, tagged "Seed dataset (T003)"). Index is now **305
    rows, 1:1 with the JSON** — no missing rows, no orphans.
- Scientific-name check: exactly **one** shared scientific name (`Epipremnum aureum`, for both
  **Pothos** and **Golden Pothos**) — an intentional everyday-name alias, not a true duplicate.
  Recorded in `docs/LIMITATIONS.md` under the existing care-DB row (which flagged "checked
  again at T131").

### Docs (same commit)
- `TASKS.md` T131 box ticked `[x]`.
- `README.md` T131 status row → ✅ done (305 species, deduped & validated, provenance complete).
- `docs/LIMITATIONS.md` — added the T131-audit note to the care-DB limitation row.

### Checks run
- `xcodegen generate` → clean.
- `xcodebuild test … -destination 'iPhone 17 Pro'` → **233 tests, 0 failures**. Includes
  `CareDatabaseTests.testShippedDatabaseDecodesAndValidates` (T004 validator over the real file).
- `./build_run.sh "iPhone 17 Pro"` → **BUILD SUCCEEDED**, screenshot written.

### Outcome
Done. Done-when satisfied: ≥ 290 unique species (305), T004 validator passes on the whole
dataset, Provenance index has a row per plant, README updated. No remaining work.
