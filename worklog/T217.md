# T217 — Rename "Name" field label to "Nickname" everywhere

## 2026-06-01

- **Scope review:** Searched `Sources/` for user-facing "Name" plant-label UI.
  - `PlantEditView.swift:25` — `Section("Name")` (placeholder already "Nickname") → renamed section header to `Section("Nickname")`.
  - `BasketAddView.swift:88` — `TextField("Name", …)` placeholder → `TextField("Nickname", …)`.
  - `PhotoCaptureView.swift` — only *displays* `nickname`; no "Name" label, no change needed.
  - `RoomsView.swift:121` — `Section("Name")` is the **room** name, not a plant nickname → left untouched (out of scope; species/room labels untouched per Done-when).
  - `BasketAddView.swift:79` descriptive copy ("random name you can edit") is body text, not an input label → left as-is.
- **Tests:** No test asserts on these labels (`grep` of `Tests/` for "Name" labels: no matches), so none needed updating.
- **Verification:** `xcodegen generate` + `xcodebuild test` on iPhone 17 Pro sim → **238 tests, 0 failures, TEST SUCCEEDED**.
- **Docs:** Ticked `TASKS.md` box, flipped `README.md` status row to ✅. No new trade-off → `docs/LIMITATIONS.md` unchanged.

Done — no plant input is labelled "Name"; species/room labels untouched; unit tests green.
