# T003 worklog

## 2026-07-17

### Build completed successfully

**What was done:**
1. Created `Sources/DesignSystem/SproutTheme.swift` with `enum SproutTheme` holding every
   color/gradient member named in redesign-architecture.md §2's SproutTheme contract comment:
   paper, ink, brandGreen, heroGradient, logoGradient, launchGradient, the text ramp
   (textSecondary/textMuted/textHint/textTertiary/taupe), chrome (cream, deepGreenOnCream,
   cardSurface, segmentedTrack, toggleOffTrack, progressTrack, sheetScrim), semantic colors
   (destructive, swipeEdit, dueTodayAmber, warningTerracotta, sun, brightnessChip,
   softGreenFill), and the sage/oat surface sets — each with the exact hex values from
   redesign-spec.md §1.2. Added a private `Color(hex:)` init helper.
2. Added `SproutTheme.Radius` (sheet 40, hero 28, bento 24, dialog 22, row 20, field 18,
   button 16, segmented 15, pill 11, chip 10) per spec §1.3.
3. Added shadow ViewModifier helpers per spec §1.3 (card, bento, hero, primaryButton, dialog)
   exposed as `View` extension methods `.cardShadow()`, `.bentoShadow()`, `.heroShadow()`,
   `.primaryButtonShadow()`, `.dialogShadow()`.
4. Added `Tests/SproutThemeTests.swift`: asserts every `Radius` value against the spec list,
   and spot-checks 3 colors (paper, ink, brandGreen) resolve to the expected sRGB components
   via `UIColor.getRed(green:blue:alpha:)`, within 0.01 tolerance.

**Verification:**
- `xcodegen generate` — succeeded.
- `swiftlint lint` — 0 errors, 40 pre-existing warnings in unrelated files (unchanged from
  T002's baseline).
- `xcodebuild test -destination 'platform=iOS Simulator,name=Sprout-Claude'` — all 288 tests
  pass, including the 4 new `SproutThemeTests`.
- `./build_run.sh Sprout-Claude -seedDemoData YES` — build succeeded, app launched, screenshot
  captured and reviewed: Home screen renders normally (green/oat bento tiles, hero card,
  notification permission sheet) — unaffected, as expected, since this task adds tokens only
  and touches no screen files.

**Status:** ready for merge — no screen files changed; tokens-only per task scope.
