# T052 Worklog

## 2026-07-20 — Initial build

**What was done:**
- Reviewed RoomDetailView as the reference pattern for correct back button implementation
- Updated PlantDetailView (lines 22-86):
  - Added `@Environment(\.dismiss) private var dismiss` property
  - Converted toolbar leading item from a decorative Label to a functional Button calling `dismiss()`
  - Added `.navigationBarBackButtonHidden(true)` to hide the system back button
  - Added `.accessibilityLabel("Plants")` for accessibility
  - Kept brandGreen tint and 17pt-semibold styling, `.labelStyle(.iconOnly)` for the chevron
- Updated PlantListView (lines 10-123):
  - Added `@Environment(\.dismiss) private var dismiss` property
  - Wired the existing custom '‹ Home' button action (line 68) from empty stub to call `dismiss()`
  - Added `.navigationBarBackButtonHidden(true)` after `.background(SproutTheme.paper)` modifier
  - Preserved the existing ChromeIcon.chevronLeft + "Home" label and sizing

**Checks run:**
- **Lint:** `swiftlint lint` — passed (50 pre-existing warnings, 0 errors)
- **Test:** `xcodegen generate && xcodebuild test -project Sprout.xcodeproj -scheme Sprout -destination 'platform=iOS Simulator,name=Sprout-Claude'` — all 361 tests passed
- **Empirical verification:**
  - My Plants list (`SPROUT_SCREEN=plants`): Screenshot shows exactly ONE back control (green "< Home" button, top-left), no duplicate system chevron
  - Plant detail (`SPROUT_SCREEN=detail`): Screenshot shows exactly ONE back chevron (green, top-left), no duplicate system chevron
  - Both buttons are functional Button instances (verified by code inspection) calling `dismiss()` to pop the NavigationStack
  - Swipe-back gesture preserved (navigationBarBackButtonHidden does not disable interactive pop)

**Result:** `done` — Task complete, all Definition of Done criteria met.
