# T058 worklog

## 2026-07-20 — Completed

**What was done:**
- Extracted the Direct Sun / Indirect Sun / Humidity segmented picker controls into a shared `environmentLeversSection()` @ViewBuilder in AddRoomView
- Updated `presetSummarySection()` to render the editable levers (bound to $directSun / $indirectSun / $humidity) instead of read-only chips, replacing the "TYPICAL SETTINGS" card content
- Updated `customControlsSection` to reuse the shared `environmentLeversSection()`, removing duplicate code
- Both paths now show identical editable controls:
  - Direct Sun (Low/Medium/High)
  - Indirect Sun (Low/Medium/High)
  - Humidity (Dry/Normal/Moist)
  - Each wrapped in RoomInfoHeader with help text
- State binding and applyPresetDefaults() behavior preserved: changing the preset wheel re-seeds the levers to the preset's defaults
- Custom ("Other") path still shows name field above the shared levers

**Checks run:**
- `swiftlint lint` — passed with no new violations
- `xcodegen generate && xcodebuild test` — all 361 tests passed
- `./build_run.sh Sprout-Claude -seedDemoData YES` with `SPROUT_SCREEN=addroom` — built, installed, launched
- Visual verification: screenshot shows "Living Room" preset with editable Direct Sun (Medium), Indirect Sun (High), Humidity (Normal) segmented controls visible and pre-filled from preset

**Result:** done
