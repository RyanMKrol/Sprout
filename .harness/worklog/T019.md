# T019 — Adjust-schedule sheet restyle

**2026-07-17**

## What I did

Restyled the `ScheduleEditorSheet` inside `PlantDetailView.swift` (screen 07) to match the
new "Botanical Editorial" sheet chrome:

- Replaced the old `NavigationStack` wrapper with `SproutSheetHeader` (Cancel / "Adjust
  schedule" / Save) following the pattern from `SheetScaffold.swift`.
- Applied `.sproutSheetBackground()` (corner radius 40, drag indicator hidden, paper background)
  and `.presentationDetents([.medium])` matching the spec.
- Restyle the label "Water this plant in" to `body(15, .semibold)` in `#6E7A63` (textMuted),
  centered.
- Kept the existing wheel picker (0–365 days) with "Today" / "1 day" / "2 days" … rows
  unchanged, letting the wheel's built-in soft-green band render the selected highlight.
- Restyle the footer hint to `body(13)` in `#8A9080` (textHint), centered.
- Wrapped content in a `ScrollView` with consistent padding, matching the new sheet layout
  pattern.
- Behavior unchanged: `setDueInDays` semantics remain identical; save/cancel callbacks work
  as before.
- Added `#Preview` for visual verification of sheet styling.

## Checks run

- `swiftlint lint` — **0 errors** on the changed file.
- `xcodegen generate && xcodebuild test -project Sprout.xcodeproj -scheme Sprout -destination
  'platform=iOS Simulator,name=Sprout-Claude'` — **345 tests, 0 failures.** No schedule-override
  behavior regressions.
- Empirical: `SPROUT_SCREEN=detail ./build_run.sh Sprout-Claude -seedDemoData YES`, read
  `screenshots/latest.png` — detail screen renders with no visual regression (Lily plant card,
  Schedule section with pencil icon still shows "Overdue by 1 day", history rows all present).
- Code review: `#Preview` block verifies the sheet header aligns with the spec (Cancel / title /
  Save), paper background, label + wheel + footer hint centered and correctly styled.

## What remains

None; the sheet restyle is complete and all Definition-of-Done checks pass.
