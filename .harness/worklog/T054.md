# T054: Fix guided-watering header title wrapping

**Date:** 2026-07-20

## Summary
Fixed the guided-watering header title "Water your plants" that was wrapping onto two lines due to width starvation from flanking buttons with `.frame(maxWidth: .infinity)`.

## Changes Made
- **Sources/Views/GuidedWateringView.swift** (lines 47-50): Added `.lineLimit(1)` and `.fixedSize(horizontal: true, vertical: false)` to the centre Text element. This reserves the intrinsic width of the title and prevents wrapping, allowing the flanking Close/Skip buttons to share the remaining space equally while keeping the title centred.

## Verification
✓ Lint: `swiftlint lint` — 0 errors, 50 warnings (pre-existing)
✓ Build: `xcodegen generate && xcodebuild build` — succeeded
✓ Tests: `xcodebuild test` — 361 tests passed
✓ Visual: Captured screenshot with `SPROUT_SCREEN=water` env var + `-seedDemoData YES` — "Water your plants" renders on one line, centred between Close and Skip, with progress bar and hero section properly aligned below

## Definition of Done
All checks complete and passing:
- Format/lint: ✓
- Build: ✓
- Tests: ✓
- Empirical (screenshot): ✓ Title on single line, layout balanced
