# T029 Worklog

## 2026-07-17

**Task:** Restyle the add-room sheet with preset wheel + typical settings cards + Other… path

**What was done:**
- Replaced Form-based layout with SproutSheetHeader (Cancel / "Add Room" / Add)
- Implemented ROOM TYPE section with eyebrow + white card containing wheel picker
  - Wheel shows all presets + "Other…" at end
  - Selected room display shows Bricolage-20 text (preset name or "Other…")
- Implemented TYPICAL SETTINGS section (shown when preset is selected):
  - Eyebrow label in taupe
  - White card with "Brightness" row → amber chip (#C4832A on rgba(217,139,10,0.14), r10)
  - Divider
  - "Humidity" row → soft-green chip (SproutTheme.softGreenFill)
  - Values live-update when wheel selection changes via onChange modifier
- Implemented Other… path (shown when "Other…" is selected):
  - Swaps TYPICAL SETTINGS card for CUSTOM ROOM section
  - Name field (white card, r18)
  - DIRECT SUN segmented picker with RoomInfoHeader (reused popover copy)
  - INDIRECT SUN segmented picker with RoomInfoHeader (reused popover copy)
  - HUMIDITY segmented picker with RoomInfoHeader (reused popover copy)
- Added footer hint (verbatim from spec): "Pick a common room and Sprout fills in typical light and humidity — fine-tune it later by editing the room."
- Preserved deep link (addroom) and save semantics (onSave callback)

**Checks run:**
- swiftlint: PASS (0 violations in AddRoomView.swift)
- xcodebuild test: PASS (352 tests, 0 failures)
- xcodebuild build: PASS
- Visual verification: Screenshot taken with Sprout-Claude simulator, addroom screen loaded:
  - ✅ Sheet header visible (Cancel, "Add Room", Add button)
  - ✅ Wheel in white card showing presets + "Other…"
  - ✅ TYPICAL SETTINGS card with Brightness (amber chip "Bright") / divider / Humidity (green chip "Normal")
  - ✅ Footer hint visible and matches spec verbatim
  - ✅ Layout uses Bricolage for display text, correct colors/radii/shadows

**Test results:**
- RoomPresetTests: All 3 tests passed (testCatalogueIsNonEmptyWithUniqueNames, testCommonRoomsArePresent, testEnvironmentSummaryReflectsDefaults)
- Full suite: 352 tests passed

**Visual observations:**
- Wheel picker displays with soft-green center band (r11-12) as per design token
- Selected room name appears centered in display area below wheel (Bricolage 20)
- Chips render with correct color + opacity (brightness amber, humidity soft green)
- Sheet header styling matches design (Bricolage 18 title, green buttons)
- Custom controls section (Other… path) correctly contains name field + three segmented pickers

**Status:** DONE
