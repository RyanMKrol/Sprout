# T023 — Add flow: basket step restyle

## Completed 2026-07-17

### Summary
Restyled the basket step (screen 11) of the add flow to match the redesign spec, and wired
the icon picker onto basket-row token taps for still-pending (unsaved) entries.

### Changes
- **AddFlowView.swift**:
  - Replaced the `List`-based basket step with a custom scrollable sheet layout: header
    (`SproutSheetHeader`, Cancel / "Add Plants"), room summary row (house bubble + name +
    "Change"), "BASKET · N" eyebrow with compact white rows (34pt token + nickname + italic
    species + green shuffle), "ADD SPECIES" eyebrow with a tinted search field and result
    rows (name + green circle-plus), and a pinned `SproutPrimaryButtonStyle` "Add N Plants"
    button (disabled until `canCommit`) replacing the old toolbar confirm button.
  - Tapping a basket row's token now presents `IconPickerView` for that entry via
    `sheet(item:)`; the picker's `onSave` calls the new `viewModel.updateIcon(_:for:)`.
  - Added `FixedGlyphPlantToken`: `PlantToken`'s glyph isn't `.resizable()`, so on the
    bundled (non-SF-Symbol) Phosphor assets it renders at native size and spills across the
    row — discovered via the required screenshot check. `HomeComponents.swift` already
    carries the identical workaround for the same reason (`HomePlantAvatar`); `PlantToken`
    itself is in `Sources/DesignSystem/`, out of this task's scope, so the fix is a local
    token view (used here and in `IconPickerView`'s 88pt preview) rather than an edit to it.
- **IconPickerView.swift**: generalised from a `Plant`+`repository`-only sheet to work over
  a preview identity (name/species/token-id) with an `onSave(PlantIcon)` closure, so it can
  open on a persisted plant (existing `plant:repository:` init, unchanged behaviour) **or**
  a pending basket entry (new `icon:name:species:tokenID:onSave:` init). Also swapped its
  preview token to `FixedGlyphPlantToken` (same bug as above).
- **BasketAddViewModel.swift**: `Entry` gained an `icon: PlantIcon` field (defaulted to
  `PlantIcon.default(forSpecies:)` at `add()`, same as `Plant`'s own default), a new
  `updateIcon(_:for:)` mutator, and `commit()` now passes `entry.icon` into the created
  `Plant` so a picked icon survives the batch commit.
- **Tests/BasketAddViewModelTests.swift**: 3 new tests — `add()` assigns the species default
  icon, `updateIcon` changes a pending entry's icon, and `commit()` carries that icon into
  the created `Plant`.

### Checks run
- **Lint**: `swiftlint lint` — 0 errors (pre-existing warnings only, plus one new advisory
  `type_body_length` on `AddFlowView`, matching the file's existing pattern).
- **Test**: `xcodegen generate && xcodebuild test … -destination 'platform=iOS Simulator,
  name=Sprout-Claude'` — 350/350 tests pass (347 existing + 3 new).
- **Empirical**: `./tools/loop_sim.sh` → `SPROUT_SCREEN=basket` →
  `./build_run.sh Sprout-Claude -seedDemoData YES`, read `screenshots/latest.png`. Confirmed:
  Cancel / "Add Plants" header; white "Bathroom" room-summary row with house bubble +
  "Change"; "BASKET · 3" with three white rows, each a correctly-sized 34pt circular token
  (fixed from the initial spill-over render), bold nickname, italic species, and a green
  shuffle icon; "ADD SPECIES" eyebrow, tinted search field, and species rows with a green
  circle-plus; pinned green "Add 3 Plants" button at the bottom. Verified the row's icon-tap
  wiring by code review (`sheet(item: $editingIconEntry)` → `IconPickerView` →
  `viewModel.updateIcon`) — no interactive tap-driver tool was available this session to
  screenshot the picker sheet itself mid-flow.

### Notes
- Kept the species display as `entry.species.capitalisedWords` in italic (matching the
  existing precedent from before this restyle) — the spec's "⚑ derived" rule about showing
  a separate Latin/scientific name would need a new `CareProfile` field, which is a
  `Sources/Model/` change outside this task's scope.
- Basket rows no longer have swipe-to-delete (the old `List`'s `.onDelete`) — the mockup's
  compact row only specifies token/name/species/shuffle, and the flow is a custom
  `ScrollView`/`VStack` now rather than a `List`. `BasketAddViewModel.remove(_:)` /
  `remove(atOffsets:)` are unchanged and still covered by existing tests; only the view's
  affordance for calling them was dropped, to stay spec-accurate. Flagging as a possible
  follow-up if product wants an explicit remove action back.
