# T012 Worklog

## 2026-07-17

**Completed successfully.**

### What was done
- `Sources/Persistence/StoredModels.swift`: added `StoredPlant.iconName: String?` (optional →
  lightweight migration, no `VersionedSchema`). `StoredPlant(domain:)` writes
  `plant.icon.rawValue`; `toDomain()` decodes via `iconName.flatMap(PlantIcon.init(rawValue:)) ??
  PlantIcon.default(forSpecies: species)`; `applyScalars(from:)` carries it through updates.
- `Sources/Model/Plant.swift`: added `Plant.icon: PlantIcon`. The initializer takes `icon:
  PlantIcon? = nil` and defaults to `PlantIcon.default(forSpecies: species)` when omitted, so
  every existing call site (all keyword-arg based) keeps compiling unchanged.
- `Sources/DemoSeed.swift`: gave the 5 demo plants distinct icons — Lily `.flower`, Monty
  `.plant`, Fern Bundy `.leaf`, Pothos Pete `.pottedPlant`, Spike `.cactus`.
- `PlantIcon` itself already existed (`Sources/DesignSystem/SproutIcon.swift`, out of this
  task's scope) with the 16 Phosphor cases and `PlantIcon.default(forSpecies:)` — this task wired
  it onto the model/persistence layer only, per spec.
- `Tests/PersistenceTests.swift`: added
  - `testPlantIconRoundTrips` — a plant saved with `.cactus` reads back with `.cactus`.
  - `testUpdatePersistsIcon` — `repo.update` persists a changed icon.
  - `testStoredPlantWithNilIconNameFallsBackToSpeciesDefault` /
    `testStoredPlantWithGarbageIconNameFallsBackToSpeciesDefault` — construct a `StoredPlant`
    directly with `iconName: nil` / an unrecognised string and assert `toDomain().icon` falls
    back to `PlantIcon.default(forSpecies:)`.
  Wrote these against the existing (pre-change) code first to confirm they failed for the right
  reason (no `icon`/`iconName` in scope), then implemented until green.

### Checks run
- `swiftlint lint` — 0 errors (46 pre-existing advisory warnings, none new).
- `xcodegen generate && xcodebuild test -project Sprout.xcodeproj -scheme Sprout -destination
  'platform=iOS Simulator,name=Sprout-Claude'` — **319 tests, 0 failures**, including the 4 new
  icon tests.

### What remains
Nothing outstanding for this task. No UI files were touched (model/persistence/demo seed +
tests only), matching "Done when".
