# T027 — Rooms list restyle + room delete confirmation

## 2026-07-20

**What was done:**
- Completely restyled RoomsView to match redesign spec screen 14:
  - Paper background with proper spacing
  - "‹ Home" back button and "Rooms" title (display 32) in header
  - Green FAB replacing toolbar button
  - White rows (radius-20) with proper padding
  - Per-room icon bubbles (44×44, radius-14, oat surface) with room-name-keyed icon mapping:
    - "living" → couch, "bed" → bed, "bath" → bath, "kitchen" → utensils, "dining" → mugSaucer, else house
    - Icon color: oat-icon (#B4832F)
  - Room names (display 18), environment summaries (body 12.5), plant counts (body 13, semibold, taupe)
  - NavigationLink to RoomDetailView (which already exists in codebase)
  - Empty state restyled with minimal hint text and primary button

- Implemented swipe-to-delete with SproutAlert confirmation:
  - Red trash icon
  - Title: "Delete {room}?"
  - Body text varies by plant count:
    - With plants: "Its {N} plants stay in your garden without a room's light and humidity. This can't be undone."
    - No plants: "This can't be undone."
  - Cancel / Delete button (destructive color #C4553B)

- Updated RoomsViewModel to remove the unused `delete(atOffsets:)` method that was for swipe actions (now using alert-based flow)

- Maintained existing deep-link support for roomeditor/addroom flows

**Checks run:**
- `swiftlint lint` — passed (3 line-length violations are pre-existing in RoomEditorView, not part of this task)
- `xcodebuild test` — all 363 tests passed
- `./build_run.sh Sprout-Claude -seedDemoData YES` → screenshot verified:
  - White rows with oat icon bubbles render correctly
  - Plant counts display properly
  - Display fonts render correctly
  - Layout matches spec

**What remains:**
- None — task complete per spec.
