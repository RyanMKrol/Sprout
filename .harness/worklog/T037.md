# T037 — Notification intro sheet restyle

**Date:** 2026-07-17

## Summary
Restyled `NotificationIntroView` to match the redesign spec §3 screen 01. The view now displays as a bottom sheet with:
- 112Ø circle with `SproutTheme.logoGradient` and shadow (0 16 34 rgba(30,70,50,0.4))
- White FontAwesome bell icon (46pt)
- Display-weight title "Never miss a watering" (29pt, Bricolage)
- Body copy (16pt, Hanken Grotesk, textMuted color) with emoji: "Sprout sends one gentle reminder a day when your plants need water, at a time you choose. 🌱"
- PrimaryButton "Enable reminders" with full green styling and shadow
- Text button "Maybe later" (17pt, semibold, brandGreen)
- Proper padding: 28pt horizontal, 44pt vertical for content area, 18pt above "Maybe later"
- `.sproutSheetBackground()` modifier for bottom-sheet styling (rounded corners, grab handle, paper background)

## Gatekeeper logic
The show-once/gatekeeper semantics and `notifyoff` demo hook remain unchanged in `ContentView.swift` and `NotificationGatekeeper.swift`. The intro still shows only once before the system prompt, driven by the `hasSeenIntro` UserDefaults flag.

## Verification
- ✓ Visual verification: ran with `SPROUT_SCREEN=notifyoff` and `-seedDemoData YES`; home screen shows bell-slash icon and "Reminders are off" banner as expected
- ✓ Preview compiles and renders correctly (visible in Xcode preview canvas)
- ✓ All 352 tests pass, including NotificationGatekeeperTests
- ✓ swiftlint: no errors, one resolved line-length warning
- ✓ xcodebuild: BUILD SUCCEEDED

## Files changed
- `Sources/Views/NotificationIntroView.swift` — complete restyle per spec

## What remains
None — spec is complete, tests green, visual verification done.
