# T017 — My Plants list restyle

## 2026-07-17

### What was built

Redesigned PlantListView to match the redesign spec (§3 screens 04/05):

1. **View structure**: VStack with custom header + List body
   - Header: "‹ Home" back button (green, 17pt body w/medium), "My Plants" display(32), green SproutFAB (+) top-right
   - Paper background throughout
   - List using `.plain` style, `listRowBackground(paper)`, `listRowSeparator(.hidden)`

2. **Plant row cards** (PlantCardView):
   - White `.sproutCard(radius: 20)` with padding 13×14
   - HStack: PlantToken(46pt, photo wins) + VStack(name display(18) + species bodyItalic(12.5) + meta body(11.5 semibold taupe))
   - Trailing DueChip (colored by status: red/amber/green)
   - Cadence metadata shows "Every Nd" + " · shortened"/" · stretched" derived from whySummary

3. **Empty state** (PlantListEmptyState):
   - 84pt green flower token
   - "No plants yet" display(22)
   - Body copy verbatim (14.5pt)
   - "Add your first plant" primary button

4. **Delete flow**:
   - Swipe Delete action shows `SproutAlert` confirmation before deleting
   - "Delete {name}?" with plant name, message + Cancel/Delete buttons
   - Uses existing `viewModel.delete(id:)` on confirmation

5. **Deep links preserved**: all existing routes (`add`, `basket`, `addflow`, `edit`, `camera`, `photoprompt`) still work via deepLinkIfRequested()

6. **Accessibility**: combined label with name + species + due status + why summary

### Verification

- **Tests**: All 353 tests passing (PlantListViewModel tests included)
- **Lint**: One advisory file-length warning (424 lines, threshold 400) — acceptable
- **Visual**: `./build_run.sh Sprout-Claude -seedDemoData YES` with `SPROUT_SCREEN=plants` produces screenshot showing:
  - Paper background ✓
  - Display-font title + green back/FAB ✓
  - White token rows with italic species + colored due chips ✓
  - Sorted soonest-due first ✓

### What remains

None — task complete.
