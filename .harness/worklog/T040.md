## 2026-07-17 T040

Re-attempt of T006 (blocked on a name collision — see `.harness/worklog/T006.md`). This
attempt's scope was widened to `Sources/ViewModels/**`, `Sources/Views/**`, and
`Sources/ContentView.swift`, giving room to resolve the collision instead of working around it.

What I did:
1. Renamed the legacy v1 `enum DueStatus` in `Sources/ViewModels/PlantListViewModel.swift` to
   `WateringDueStatus`, keeping its cases, `init(nextDue:now:calendar:)`, `label`, and
   `needsWater` verbatim — only the type name changed.
2. Updated every reference to the old name: `Sources/ViewModels/PlantListViewModel.swift`,
   `Sources/ViewModels/PlantDetailViewModel.swift`, `Sources/ContentView.swift`, and the two
   existing unit test files (`Tests/PlantListViewModelTests.swift`,
   `Tests/PlantDetailViewModelTests.swift`). `Sources/Views/PlantListView.swift` and
   `Sources/Views/PlantDetailView.swift` never named the type directly (only via inferred
   `.due` properties), so no change was needed there.
3. Added `Sources/DesignSystem/DueChip.swift` with the new design-system `enum DueStatus`
   (`overdue(days:)`, `dueToday`, `due(inDays:)`, `unscheduled`), its `label`/`tint`/`background`
   per redesign-spec §1.2/§2 item 2, and the `DueChip(status:)` view (SproutFont.body(11,.bold),
   padding 5×11, RoundedRectangle corner radius 10).
4. Added `Tests/DueChipTests.swift` asserting the five exact label strings from "## Done when"
   (test-first: written before `DueChip.swift`, confirmed it failed to compile since
   `DesignSystem.DueStatus` didn't exist yet, then implemented).

Checks run (all green):
- `xcodegen generate` — succeeded.
- `swiftlint lint` — 0 errors, 50 pre-existing warnings (none introduced by this change).
- `xcodebuild test -project Sprout.xcodeproj -scheme Sprout -destination 'platform=iOS Simulator,name=Sprout-Claude'`
  — 353 tests, 0 failures, including `DueChipTests.testLabels` and all pre-existing
  `WateringDueStatus`/`PlantListViewModelTests`/`PlantDetailViewModelTests` cases.
- Visual/empirical: `./tools/loop_sim.sh && ./build_run.sh Sprout-Claude -seedDemoData YES`
  after a clean DerivedData wipe — build succeeded, app launched, Home screen (`screenshots/latest.png`)
  renders correctly with demo data, no crash or regression from the `WateringDueStatus` rename.
  `DueChip` itself isn't wired into any screen yet — that's screen-integration work the spec
  doesn't ask this task to do (component-only task); it's covered by design-system unit tests
  and the `#Preview`.

What remains: wiring `DueChip`/`DueStatus` into `PlantRow` (§2 item 12) is a separate task.
