# T050 Worklog

## 2026-07-20

**Task:** Fix broken rounded corners on ghost buttons and delete card by replacing `.border(...).cornerRadius(...)` anti-pattern with overlay-stroked RoundedRectangle.

**What was done:**
1. Fixed `SproutGhostButtonStyle` in `Sources/DesignSystem/Buttons.swift` (lines 33-37):
   - Replaced `.border(Color(...), width: 1.5).cornerRadius(18)` with `.overlay(RoundedRectangle(cornerRadius: 18, style: .continuous).stroke(...))` + `.clipShape(RoundedRectangle(cornerRadius: 18, style: .continuous))`
   - Preserved the exact green color RGB(47/255, 107/255, 76/255, opacity: 0.32) and 1.5px stroke width

2. Fixed delete card in `Sources/Views/PlantEditView.swift` (lines 163-170):
   - Replaced `.background(Color.white.border(...)).cornerRadius(SproutTheme.Radius.row)` with `.background(Color.white).overlay(RoundedRectangle(...).stroke(...)).clipShape(RoundedRectangle(...))`
   - Preserved the exact destructive-tint color RGB(196/255, 85/255, 59/255, opacity: 0.2) and 1px stroke width and SproutTheme.Radius.row radius

3. Verified no `.border(` calls remain anywhere in Sources/ via grep.

**Checks run:**
- `swiftlint lint`: PASS (no new errors, 50 pre-existing warnings)
- `xcodegen generate && xcodebuild test`: PASS (361/361 tests pass)
- `./build_run.sh Sprout-Claude -seedDemoData YES`: PASS (build succeeded, app launches)

**Visual verification:**
Screenshot (screenshots/latest.png) shows the home screen with the fixed ghost buttons ("Add a plant" and "Check in") rendering with clean, fully-rounded corners and no square notches or broken stroke. The 1.5px green outline sits cleanly on the rounded edge as intended.

**Result:** done
