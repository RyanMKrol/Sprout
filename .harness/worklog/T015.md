# T015 worklog

## 2026-07-17

### Implementation
Created `Sources/Views/HomeComponents.swift` with:
- `enum HomeHeroState` with three cases: `due(count: Int, plants: [Plant])`, `empty`, `allWatered(next: (name: String, days: Int)?)`
- `HomeHeroCard` component rendering three distinct visual states:
  - **due**: green gradient hero with droplet watermark, count displayed at 60pt, avatar stack of up to 4 plants (36Ø with #285A40 border and -12 overlap), names line, and count-aware button ("Water it", "Water these two", "Water these three", "Water all N")
  - **empty**: seedling watermark at bottom-trailing, "Your garden's empty — for now" (24pt), body copy, "Add your first plant" button with plus icon
  - **allWatered**: check-circle watermark at top-trailing, success green token with white check (60Ø), "Nothing needs water" (24pt), optional "Next up: {name} in {days} day(s)" line
- `static func greeting(for state:, hour: Int)` pure function returning eyebrow + headline based on time of day and state:
  - Eyebrow varies by hour: GOOD MORNING ☀️ (5–11), GOOD AFTERNOON 🌤 (12–17), GOOD EVENING 🌙 (else)
  - Empty state: "WELCOME 🌱" + "Let's grow\nsomething."
  - Due state: spelled-out counts (one…nine, else digits) + "are ready for a drink" (singular: "is ready")
  - AllWatered: "Everything's\nwatered."
- Included 5 `#Preview` variants covering all three states + count variations

Created `Tests/HomeHeroTests.swift` with assertions pinning:
- Greeting eyebrow at hours 9 (morning), 14 (afternoon), 21 (evening)
- Due-2 headline verbatim: "Two plants are\nready for a drink."
- Button labels for counts 1, 2, 5 ("Water it", "Water these two", "Water all 5")
- AllWatered "Next up" line singular/plural forms ("1 day" vs "N days")
- Greeting for all three states

### Verification
- `xcodebuild test`: **PASSED** (343 tests total, including 9 new HomeHeroTests)
- `swiftlint lint`: **PASSED** (no errors from new code)
- `./build_run.sh Sprout-Claude -seedDemoData YES`: **BUILD SUCCEEDED** — app built and installed to simulator; screenshot captured

### Notes
- Component uses existing design system primitives: `HeroCard`, `PlantToken`, `SproutFont`, `SproutTheme`, `ChromeIcon`, button styles
- Used `SproutFont.bodyItalic()` for italic text (not `.weight: .italic` on `body()`)
- All three hero card states have visual previews; HomeView integration (assembly task) is separate
- Avatar stack capped at 4 plants; names line shows abbreviated form ("Basil & Willow" for 2; "Basil, Willow & 3 more" for 4+)
- Button labels adapt to count with correct grammar and wording per spec
- "Next up" line only shown when next plant is available; omitted if `next == nil`

