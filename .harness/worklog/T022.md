# T022 — Add flow: room step restyle

## Completed 2026-07-17

### Summary
Restyled the room step of the add flow to match the new design specification with a full-height sheet presentation.

### Changes
- **AddFlowView.swift**: Replaced the old List-based room step with a custom full-height sheet layout featuring:
  - Handle and right-aligned Cancel button in header
  - Display title "Where do they\nlive?" in Bricolage 28
  - Body copy with spec-correct color (#6E7A63)
  - "YOUR ROOMS" eyebrow section
  - White radius-18 room rows with oat-tinted house icon bubble (40×40 r12), room name (display 17), environment summary (body 12.5), and chevron
  - GhostButton for "+ Add a new room"
  - Text button for "Skip — no room for now" in correct color (#8A9080)
  - Removed NavigationStack wrapper since the view is already presented in a sheet

### Checks passed
- ✅ Lint: `swiftlint lint` — 0 errors, pre-existing warnings only
- ✅ Tests: `xcodebuild test` — all 347 tests pass (BasketAddViewModel tests included)
- ✅ Build: `xcodebuild build` — full build successful
- ✅ Visual verification: Screenshot with `SPROUT_SCREEN=addflow` shows properly styled room step matching spec exactly

### Observations
The visual output shows all design elements correctly:
- Paper background applied correctly
- Typography hierarchy matches spec (display 28 for title, body 14 for copy)
- Color palette accurate (#6E7A63 for body, #B4832F for icons, #8A9080 for skip button)
- Layout spacing and radii correct (r18 for rows, r12 for icon bubble)
- Interaction preserved: tapping room rows advances to plants step, buttons work as specified
- Staged-dismissal behavior and addflow deep-link maintained

No behavioral changes to the underlying view model — all existing functionality preserved.
