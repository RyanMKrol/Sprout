# T005 — PlantToken view + PlantTokenPalette — worklog

## 2026-07-17 — Implementation complete

**What was done:**
1. Created `Sources/DesignSystem/PlantToken.swift` with:
   - `PlantTokenPalette` struct with 6 color duos (green, purple, blue, gold, teal, pink) + success
   - `duo(for id: UUID)` deterministic hashing using the same strategy as `PlantPalette.color(for:)` (sum Unicode scalar values, mod 6)
   - `PlantToken` view with photo-wins rule: displays UIImage clipped in Circle when present, else RadialGradient + white glyph
   - Radial gradient with center offset to 30%/25%, radius 75% of size, white glyph at 45% of size, shadow with dark color at 0.28 opacity, radius 5, y 2
   - Lightweight preview block showing all token variants

2. Created `Tests/PlantTokenTests.swift` with:
   - `testDuoIsStable` — verifies same UUID always yields same duo across calls
   - `testDistinctUUIDsMapToPredictableDuos` — pins two fixed UUID literals to their computed duos, verifying hash determinism

**Checks run:**
- `swiftlint lint` — passed, no warnings
- `xcodebuild test` — passed with 311 tests (309 existing + 2 new PlantTokenTests)
- `./build_run.sh Sprout-Claude -seedDemoData YES` — app builds, installs, and launches; screenshot captured

**Observation:**
PlantToken compiles and renders correctly. The preview shows the six gradient token variants with various plant icons. The component is available for use by downstream screens but is not yet integrated into any existing view (per spec: "no existing screen uses it yet").

**Status:** DONE
