# T033 — Guided watering: report + action screens restyle

## 2026-07-17

### Work completed
1. Read redesign-spec §3 screens 20/21 and existing coordinator/view code
2. Rearchitected GuidedWateringView to match new full-cover design:
   - Replaced Form-based layout with full-screen ZStack on paper background
   - Implemented header bar with "Close" (left), "Water your plants" (center), "Skip" (right, green text)
   - Added 5pt progress bar with #E1DBCB track + brandGreen fill, proportional to position/total
   - Report step: hero section with PlantToken (120) + nickname (display 22) + species (bodyItalic) in "{species} · {i} of {n}" format
   - Two SproutSegmentedPicker fields: "HOW DOES IT LOOK?" (Fine/Droopy) and "HOW'S THE SOIL?" (Dry/Moist/Wet)
   - Pinned PrimaryButton "Check" at bottom
   - Action step: same header/progress/hero; recommendation card using .sproutCard(radius: 22) with 78Ø tinted circle + 34 icon + headline (display 20) from RecommendationPresentation
   - Pinned buttons: "💧 I watered it" (PrimaryButton) + "Didn't water — next" (text button, green, semibold)
   - Completion step (screen 22): 104Ø success token (white check), "All done 🌿" headline, "You've been through every plant that needed water today." body, "Done" button
3. Coordinator logic untouched: modes, advance rules, "schedule only advances on a real water" preserved
4. Maintained all deep-link + identifier semantics

### Checks run
- Lint: 4 warnings (style-only, trailing closure syntax in Button calls—acceptable per existing codebase practice)
- Tests: GuidedWateringCoordinator (7 tests) all pass
- Build: ✓ no errors
- Visual: screenshot shows report step correctly rendered with all required elements (progress bar, 120 token, display name with "· 1 of N", segmented groups, pinned Check button)

### Remaining / notes
- Action and completion steps not visually tested (simulator tap not available in CLI), but code paths verified by inspection and tests
- Recommendation card correctly uses RecommendationPresentation for icon/tint/headline per redesign spec §4
- All coordinator semantics preserved; view is purely presentational restyling
