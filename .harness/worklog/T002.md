# T002 worklog

## 2026-07-17

### Build completed successfully

**What was done:**
1. Created `Sources/DesignSystem/SproutFont.swift` with three static methods:
   - `display(_:weight:)` using Bricolage Grotesque fonts
   - `body(_:weight:)` using Hanken Grotesk fonts
   - `bodyItalic(_:)` using Hanken Grotesk italic
   All use the exact PostScript names from FONTS.md verbatim.

2. Added font bundling to `project.yml`:
   - Added `Sources/Fonts` as a resource for the Sprout target
   - Added all 9 TTF filenames to the `UIAppFonts` array in Info.plist properties

3. Created `Tests/SproutFontTests.swift` with 9 test methods:
   - One test per PostScript name from FONTS.md
   - Each asserts `UIFont(name: <psName>, size: 12) != nil`
   - All tests pass, proving font registration works

**Verification:**
- `swiftlint lint` — 0 errors (40 pre-existing warnings in unrelated code)
- `xcodebuild test` — All 284 tests pass (including 9 new SproutFontTests)
- Built app bundle verified: 9 TTF files present in .app
- Info.plist verified: UIAppFonts array present with all 9 fonts
- SproutFont helpers: Not used anywhere yet (no visual changes)

**Status:** ready for merge
