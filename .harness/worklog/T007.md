# T007 — Button styles + SectionEyebrow

## Build summary

**Date:** 2026-07-17

### What was done

1. Created `Sources/DesignSystem/Buttons.swift` with three button styles and SectionEyebrow view:
   - `SproutPrimaryButtonStyle`: Green fill (brandGreen), radius 16, vertical padding 16, white body text (17, semibold), shadow with rgba(47,107,76,0.34) radius 13 y 6, pressed state scales to 0.98
   - `SproutGhostButtonStyle`: Clear fill with 1.5pt stroke (rgba(47,107,76,0.32)), radius 18, padding 14, brandGreen body text (15, semibold)
   - `SproutCreamButtonStyle`: Paper fill (#F4F1E7), radius 15, padding 14, deep green text on cream (#1C4330) body text (16, semibold)
   - `SectionEyebrow`: Uppercase text using body(11, .bold), tracking 1.4, colored with taupe

2. Added a #Preview showing all three buttons and the eyebrow for visual verification

### Checks run

- **Lint:** `swiftlint lint` — no errors (existing warnings only)
- **Build:** `xcodebuild build` — SUCCESS
- **Tests:** `xcodebuild test` — 309 tests passed, 0 failures
- **Visual verification:** `./build_run.sh Sprout-Claude -seedDemoData YES` — app launches and renders correctly

### Architecture compliance

- Style names and signatures match the architecture contract (Buttons.swift)
- No screens were modified
- All styling uses SproutTheme and SproutFont as specified
- Preview compiles and renders

### Result

All Definition of Done checks passed. Project builds, tests pass, and new components are ready for use in downstream redesign tasks.
