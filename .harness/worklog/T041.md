# T041 Worklog

## 2026-07-17

### Overview
Implemented the animated launch splash screen for Sprout, completing the re-attempt of T014 with the expanded scope that now includes asset catalog entries.

### What was done

1. **Created LaunchSplashView.swift** — A full-bleed animated splash screen featuring:
   - Full-bleed launch gradient background (#2F6B4C → #173726, 165deg)
   - Soft radial white glow (300Ø, white@0.08) positioned at upper-center
   - Centered column layout with:
     - 114×114 tile with 32pt radius (white@0.12 fill, white@0.16 border)
     - Floating animation: ±5px vertical motion over 3s ease-in-out, repeating forever
     - ChromeIcon.seedling (58pt) in cream color inside the tile
     - "Sprout" wordmark (36pt Bricolage, #F4F1E7)
     - 26Ø spinner at 72pt from the bottom with rotating cream arc over white@0.22 background ring, 0.9s linear rotation
   - All animations use keyed `.animation(..., value:)` idiom with proper state bindings

2. **Integrated splash into ContentView** — Modified ContentView.swift to:
   - Add `@State private var showLaunchSplash = true` for managing splash visibility
   - Wrap HomeView in a ZStack with the splash overlay
   - Fade out the splash (~0.35s duration) starting 0.6s after appear using `.task` and `Task.sleep(nanoseconds:)`
   - Skip the delay entirely when `DemoSeed.isActive` so screenshots/tests never race the animation

3. **Created LaunchBrandGreen color asset** — Added `Sources/Assets.xcassets/LaunchBrandGreen.colorset/Contents.json`:
   - Color value: #2F6B4C (matches the launch gradient brand green)
   - Proper RGB component representation for iOS asset catalog

4. **Updated Info.plist** — Modified UILaunchScreen dictionary to reference the color asset:
   - Added `<key>UIColorName</key><string>LaunchBrandGreen</string>`
   - Ensures the pre-SwiftUI launch screen background color matches the gradient

### Verification

- **Build & tests:** All 357 tests pass; no build errors
- **Lint:** No new violations introduced (50 existing violations unchanged)
- **Build & screenshot (normal):** `./build_run.sh Sprout-Claude` builds successfully, screenshot shows Home view (splash has already faded out)
- **Build & screenshot (demo seed):** `-seedDemoData YES` runs without delay, immediately shows home view
- **Asset catalog:** LaunchBrandGreen colorset created correctly and wired into Info.plist
- **Scope:** All edits stay within declared scope (Sources/Views/**, Sources/SproutApp.swift, Sources/ContentView.swift, Sources/Info.plist, Sources/Assets.xcassets/**)

### Remaining
Nothing — task is complete.
