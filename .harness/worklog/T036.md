## 2026-07-17 — T036: Camera diagnostics restyle

**Did:** Restyled `Sources/Views/DiagnosticsView.swift` to the dark-terminal-on-paper
language (redesign spec §3 screen 25):
- Paper background (`SproutTheme.paper`); custom nav-bar principal title "Diagnostics"
  in `SproutFont.display(16)`; the back button is the default system chevron ("‹
  Settings"), since it's pushed from Settings.
- Refresh moved into the nav bar trailing slot as a `ChromeIcon.arrowsRotate` icon
  button (was a bottom-toolbar item).
- Terminal card: `SproutTheme.ink` (`#232821`) background, 16pt radius, monospaced
  11.5pt text with a 1.75 line-height (`lineSpacing`), built as one colorized `Text`
  per redraw — leading `HH:mm:ss.SSS` timestamps in `#7C8570`, lines containing
  "✓"/"ok" in `#8FE0A8`, lines containing "retry"/"warn" in `#E7B36A`, else `#C7D0BE`.
- Bottom toolbar: frosted bar (`rgba(244,241,231,0.92)` + `.ultraThinMaterial`,
  1px hairline top border, matching the pattern already used by `AlertCard`) with
  Share / Copy (green) / Clear (red) as icon-over-`body(11)`-label buttons.
- Behavior unchanged: Share/Copy/Clear and Refresh call the same
  `DiagnosticLog`/`UIPasteboard` methods as before; the empty state
  (`ContentUnavailableView`) is untouched aside from trimming its helper copy to
  clear a swiftlint line-length warning.

**Checks run:**
- `swiftlint lint` — 0 violations in the changed file; repo-wide lint has only
  pre-existing warnings elsewhere (0 errors).
- `xcodegen generate && xcodebuild test -project Sprout.xcodeproj -scheme Sprout
  -destination 'platform=iOS Simulator,name=Sprout-Claude'` — **352 tests, 0
  failures.**
- `./build_run.sh Sprout-Claude -seedDemoData YES` — build green, app launched.
- Empirical: drove the simulator (via `axe tap`, since the demo-seed hook has no
  `SPROUT_SCREEN=diagnostics` deep-link and this task's scope is `Sources/Views/**` +
  `Tests/**` only, not the app-level deep-link plumbing) from Home → gear icon →
  Settings sheet → "Camera diagnostics" row, and screenshotted the real terminal
  content left over from the DEBUG demo photo-capture stub run. **Observed:** dark
  `#232821` terminal card with correctly colorized lines (grey timestamps, green
  "capturing photo ok"/"camera ready ok", amber "session interrupted — will retry"/
  "warn: low light detected"), the frosted Share/Copy/Clear bottom bar, and the
  refresh icon now living in the nav bar next to the "Diagnostics" title — matches
  the spec.

**Remains:** nothing outstanding for this task.
