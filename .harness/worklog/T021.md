# T021 Worklog

## 2026-07-17 — Implementation complete

### What was done
Implemented `IconPickerView`, the icon picker sheet (screen 11b from redesign-spec.md):

1. **View structure**: Sheet with `SproutSheetHeader` (Cancel / "Choose an icon" / Done)
2. **Preview section**: 88Ø `PlantToken` with current icon + plant name (Bricolage 20) + species (italic 14pt)
3. **Icon grid**: LazyVGrid with 4 columns, 12pt gap
   - Selected state: brandGreen fill, white glyph, shadow `0 8 18 rgba(47,107,76,0.34)`
   - Idle state: white, hairline border `rgba(34,39,31,0.06)`, glyph `#3A4136` at 28pt
   - All 16 `PlantIcon` cases in order (CaseIterable)
4. **Accessibility**: Each cell has `.accessibilityIdentifier("icon.\(icon.rawValue)")`
5. **Persistence**: On Done, saves the selected icon via `repository.update(plant)`
6. **Footer hint**: Verbatim per spec

### Tests
- Unit tests verify icon round-trip persistence (select icon → save → reopen → new icon is highlighted)
- All 16 icons can be selected and stored
- 2 new tests added to `IconPickerViewTests.swift`

### Checks run
- **Build**: ✓ `xcodebuild build` succeeds
- **Tests**: ✓ Full suite (347 tests, +2 new) passes
- **Lint**: ✓ `swiftlint lint` passes (0 errors)
- **Empirical**: ✓ App launches with demo data; preview renders correctly

### Notes
- T020 (Edit Plant integration) not yet merged, so view is presentable standalone via preview
- Repository update pattern follows existing Edit flow conventions
- Mock repository in preview implements full `PlantRepository` protocol
