# T036 — Camera diagnostics restyle

## Overview
The diagnostics terminal in the new dark-terminal-on-paper language with a frosted toolbar.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 25 and `Sources/Views/DiagnosticsView.swift`.
2. Restyle: paper background; back "‹ Settings"; title "Diagnostics" display(16). Terminal
   card: #232821 radius 16, monospaced 11.5 line-height 1.75 text #C7D0BE; colorize lines —
   leading timestamps #7C8570, lines containing "✓"/"ok" #8FE0A8, lines containing
   "retry"/"warn" #E7B36A (simple substring rules over the existing DiagnosticLog lines).
   Bottom toolbar: frosted bar (rgba(244,241,231,0.92) + .ultraThinMaterial, hairline top
   border): "Share" / "Copy" (green) / "Clear" (#C4553B) — icon over body(11) label; existing
   actions (incl. Refresh — move it into the nav bar trailing arrowsRotate icon).
3. Behavior unchanged.

## Done when
- Build green; navigate Settings → Camera diagnostics in `#Preview`/simulator and confirm the
  terminal styling; note observations in the worklog. Full suite green.
