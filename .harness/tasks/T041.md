## Overview
Re-attempt of T014 (animated launch splash: green gradient, floating frosted logo tile, wordmark, spinner, fading into Home). The first attempt was blocked for scope-creep: step 4 requires a UILaunchScreen background COLOR ASSET, which means creating a `.colorset` under Sources/Assets.xcassets/, but the original scope only listed Sources/Info.plist and never included Sources/Assets.xcassets/**. The loop blocked on the first out-of-scope edit (creating LaunchBrandGreen.colorset). The scope now covers the asset catalog so the same implementation can land.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 00.
2. Create `Sources/Views/LaunchSplashView.swift` per the mock: full-bleed `SproutTheme.launchGradient`; soft radial white glow (300Ø, white@0.08) upper-center; centered column — 114×114 radius-32 tile (white@0.12 fill, white@0.16 border, shadow 0 22 50 black@0.32) containing `ChromeIcon.seedling` 58pt in SproutTheme.cream, gently floating (repeatForever translateY ±5, 3s ease-in-out, keyed `.animation(..., value:)` idiom); "Sprout" `SproutFont.display(36)` in #F4F1E7, 24 below the tile. 72 from the bottom: 26Ø spinner — Circle stroke 2.5 white@0.22 with a cream trimmed arc rotating 0.9s linear repeatForever (same keyed idiom).
3. In `Sources/SproutApp.swift`/`Sources/ContentView.swift`: overlay the splash over the root at launch and fade it out (~0.35s) 0.6s after appear (`.task { try? await Task.sleep(...) }`). Skip the delay entirely when `DemoSeed.isActive` so screenshots/tests never race it.
4. Add a color asset for the static launch-screen background: create `Sources/Assets.xcassets/LaunchBrandGreen.colorset/Contents.json` (color #2F6B4C), then reference it from the `UILaunchScreen` dict in `Sources/Info.plist` so the pre-SwiftUI frame matches the gradient. Adding an asset catalog entry is IN SCOPE now (Sources/Assets.xcassets/** is declared) — do not treat it as out-of-scope.

## Done when
App launches into the splash then fades to Home; with `-seedDemoData YES` the splash is skipped/instant. `Sources/Views/LaunchSplashView.swift` exists and is rendered as an overlay at launch. A `LaunchBrandGreen` color asset exists under `Sources/Assets.xcassets/` and is wired into the `UILaunchScreen` background in `Sources/Info.plist`. `./build_run.sh Sprout-Claude` (no seed) builds and `screenshots/latest.png` shows either the splash (gradient + tile + wordmark) or Home with no visual regression. No edits land outside the declared scope.
