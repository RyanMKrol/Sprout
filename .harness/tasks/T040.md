## Overview
Re-attempt of T006, which was blocked because the spec told the builder to declare a NEW `enum DueStatus` in Sources/DesignSystem/DueChip.swift, but an existing `enum DueStatus` (different signature/labels) already lives in Sources/ViewModels/PlantListViewModel.swift and compiles into the same module — a redeclaration error the original scope (DesignSystem + Tests only) gave no room to resolve. This re-attempt widens scope to rename the legacy enum and update its call sites, then add the new design-system DueChip + DueStatus.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §1.2 (semantic colors) + §2.2 and `.harness/docs/designs/redesign-architecture.md` §2 (DueChip contract). 2. Resolve the name collision FIRST: the existing v1 due-status enum lives in `Sources/ViewModels/PlantListViewModel.swift` (`enum DueStatus`: `unscheduled`, `overdue(days:)`, `dueToday`, `due(days:)`, plus `init(nextDue:now:calendar:)`, `label`, `needsWater`). Rename it to `WateringDueStatus` (keep its existing behaviour/labels/init verbatim — only the type name changes) and update every reference to the old name: in `Sources/ViewModels/PlantListViewModel.swift`, `Sources/ViewModels/PlantDetailViewModel.swift`, `Sources/Views/PlantListView.swift`, `Sources/Views/PlantDetailView.swift`, and `Sources/ContentView.swift`. 3. Create `Sources/DesignSystem/DueChip.swift` with the NEW design-system enum `enum DueStatus { case overdue(days: Int), dueToday, due(inDays: Int), unscheduled }` exposing `var label: String` — "Overdue 1d"/"Overdue Nd", "Due today", "Due in Nd", "No schedule" — and `var tint`/`var background: Color` (overdue #C4553B on rgba(196,85,59,0.13); today #B4832F on rgba(198,138,46,0.16); due #2F6B4C on rgba(47,107,76,0.13); unscheduled SproutTheme.textTertiary on rgba(60,66,58,0.08)). `DueChip(status:)` renders the label in SproutFont.body(11,.bold), padding 5×11, in a RoundedRectangle with corner radius 10. 4. Add `Tests/DueChipTests.swift` asserting the four `DueStatus.label` strings exactly, including day interpolation ("Overdue 1d", "Overdue 3d", "Due in 5d", "Due today", "No schedule"). Existing tests for the renamed `WateringDueStatus` must keep passing (update any test references to the old `DueStatus` name too).

## Done when
`xcodegen generate && xcodebuild test` passes on the Sprout-Claude simulator, including a new `DueChipTests` whose assertions confirm `DueStatus.label` produces exactly "Overdue 1d", "Overdue 3d", "Due today", "Due in 5d", and "No schedule"; the project compiles with no redeclaration error (the two enums coexist as `DesignSystem.DueStatus` and the renamed `WateringDueStatus`); and all pre-existing tests still pass.
