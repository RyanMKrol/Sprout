# T035 — Settings sheet restyle + delete-all confirmation

## Overview
Settings in the new language, with the destructive developer action behind the new confirm.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 24 and `Sources/Views/SettingsView.swift`
   + `Sources/ViewModels/SettingsViewModel.swift`.
2. Restyle: sheet (spacer) / "Settings" / Done on paper. "REMINDERS" eyebrow; white r16 row
   "Reminder time" body(17) + trailing soft-green chip (rgba(47,107,76,0.1) r10) showing the
   time in green body(17,.semibold) — row opens the existing DatePicker (inline expand or
   sheet, keep current mechanism restyled). Helper verbatim: "Watering reminders arrive at this
   time on the day a plant is due — pick a window you're usually home." "DEVELOPER" eyebrow;
   white card rows with icons: "Camera diagnostics" (fileLines, chevron → diagnostics), "Send a
   test reminder (5s)" (bell), "Delete all plants & rooms" (trash, #C4553B). Helper verbatim:
   "Diagnostics capture camera issues. The test reminder fires in 5 seconds. Deleting is
   permanent."
3. "Delete all plants & rooms" now presents `SproutAlert`: "Delete everything?" / "Every plant,
   room and check-in will be removed. This can't be undone." / Cancel / Delete(#C4553B) →
   existing wipe path.
4. Keep the `settings` deep link + identifiers.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN settings;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: REMINDERS
  row with green time chip, DEVELOPER card with three icon rows (delete row red).
- SettingsViewModel tests green; wipe goes through the confirm.
