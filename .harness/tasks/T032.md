# T032 — Check-in recommendation restyle (unified voice)

## Overview
The check-in result phase using RecommendationPresentation: tinted icon circle, one-voice
headline, and the next-watering row.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 19 + §4, and the result section of
   `Sources/Views/CheckInView.swift`. `RecommendationPresentation` (T013) exists — consume it,
   do not duplicate mapping logic.
2. Restyle: header (spacer) / "Recommendation" / Done. Centered: 100Ø circle in
   `presentation.tint` at 0.12 + `presentation.icon` at 46×iconScale in full tint; headline
   `presentation.headline` display(23) ink centered (lh 1.25). "NEXT" eyebrow; white r16 row:
   "Next watering" body(17) / next-due date display(17) brandGreen (existing formatter).
3. Replace the old per-outcome icon/color switch in the view with `RecommendationPresentation
   .present(...)`. Keep dismissal/refresh behavior and identifiers.

## Done when
- Full suite green (CheckInViewModel + RecommendationPresentation tests).
- Visual: the `checkin` flow's result can't be reached by env var alone — verify via `#Preview`
  rendering .present for two different outcomes (water-now + skip) and record what you see in
  the worklog; the `checkin` input screenshot must show no regression.
