# T004 — Icon enums: PlantIcon (16 Phosphor) + ChromeIcon (FontAwesome)

## Overview
Typed access to the 49 bundled icon imagesets (already in
`Sources/Assets.xcassets/RedesignIcons/`), plus the default-icon-for-species rule.

## Do
1. Read `.harness/docs/designs/redesign-architecture.md` §2 (SproutIcon contract) and
   `.harness/docs/designs/redesign-spec.md` §1.4.
2. Create `Sources/DesignSystem/SproutIcon.swift`:
   - `enum PlantIcon: String, CaseIterable, Codable` with EXACTLY the 16 cases/rawValues from the
     architecture contract (`ph-flower` … `ph-pepper`); `var image: Image` returning
     `Image(rawValue).renderingMode(.template)`.
   - `static func `default`(forSpecies:)` — lowercase keyword mapping: contains "cact"/"euphorb"→.cactus,
     "palm"→.treePalm, "fern"→.leaf, "orchid"/"lotus"→.flowerLotus, "tulip"→.flowerTulip,
     "violet"/"cyclamen"/"begonia"→.flower, "ficus"/"tree"→.tree, "pine"/"conifer"→.treeEvergreen,
     "herb"/"basil"/"mint"→.grains, "citrus"/"cherry"→.cherries, "carrot"→.carrot, "pepper"/"chili"→.pepper,
     "clover"/"oxalis"→.clover, "succulent"/"echeveria"/"crassula"/"aloe"/"haworthia"→.pottedPlant,
     else→.plant.
   - `enum ChromeIcon: String` with a case per `fa-*` imageset that exists (seedling, gear, bell,
     bellSlash, droplet, plus, listCheck, chevronLeft, chevronRight, pencil, trash, house, couch,
     bed, bath, sun, arrowTrendUp, magnifyingGlass, circlePlus, shuffle, camera, circleCheck, check,
     arrowRight, lock, arrowsRotate, arrowUpFromBracket, copy, fileLines, circleInfo, xmark,
     utensils, mugSaucer) and `var image: Image` (template).
3. Add `Tests/SproutIconTests.swift`: for every `PlantIcon` and `ChromeIcon` case assert
   `UIImage(named: rawValue) != nil` (proves each imageset resolves); assert
   `PlantIcon.default(forSpecies: "Boston Fern") == .leaf` and `... "Monstera deliciosa") == .plant`.

## Done when
- `xcodebuild test` passes including `SproutIconTests` (all 49 assets resolve by name).
- Enum cases/rawValues match the architecture contract exactly.
