## Overview
Re-attempt of blocked T035. The original restyled SettingsView but was hard-blocked for scope-creep on its FIRST out-of-scope edit to Sources/DesignSystem/SproutFont.swift, which the narrow scope (Views/ViewModels/Tests only) forbade. Every DesignSystem primitive this screen needs already exists (SproutAlert + .sproutAlert modifier in AlertCard.swift, SproutFont.body(_:weight:) incl. .semibold, existing eyebrow via .tracking().textCase(.uppercase) as used in AddRoomView, cards/paper), so the DesignSystem edit was almost certainly unnecessary — but the scope left no safety valve. This re-attempt widens scope to include Sources/DesignSystem/** AND directs the work to REUSE existing primitives so the restyle actually lands.

## Do
Restyle Sources/Views/SettingsView.swift into the new visual language per redesign-spec.md §3 screen 24, REUSING existing DesignSystem primitives — do NOT add new font helpers or a new alert component; the ones you need already exist. Concretely: 1) Read redesign-spec.md §3 screen 24, SettingsView.swift, SettingsViewModel.swift, and the DesignSystem primitives AlertCard.swift (SproutAlert / .sproutAlert), SproutFont.swift (body/display), and how AddRoomView.swift renders eyebrow labels. 2) Sheet layout on paper: top spacer / 'Settings' title / 'Done' button. 'REMINDERS' eyebrow; white r16 row 'Reminder time' in body(17) with a trailing soft-green chip (rgba(47,107,76,0.1) r10) showing the time in green body(17,.semibold); the row opens the existing DatePicker (keep the current inline/sheet mechanism, restyled). Helper text verbatim: 'Watering reminders arrive at this time on the day a plant is due — pick a window you're usually home.' 3) 'DEVELOPER' eyebrow; white card rows with leading icons: 'Camera diagnostics' (icon fileLines/doc.text, chevron, navigates to DiagnosticsView), 'Send a test reminder (5s)' (bell), 'Delete all plants & rooms' (trash, color #C4553B). Helper text verbatim: 'Diagnostics capture camera issues. The test reminder fires in 5 seconds. Deleting is permanent.' 4) Replace the current confirmationDialog on 'Delete all plants & rooms' with the existing SproutAlert component: title 'Delete everything?', message 'Every plant, room and check-in will be removed. This can't be undone.', Cancel + Delete(#C4553B) confirm wired to the existing deleteAllData() wipe path. 5) Preserve the existing `settings` deep link and accessibility identifiers. If (and only if) a DesignSystem primitive is genuinely missing, extend it minimally in Sources/DesignSystem/** rather than inlining a bespoke copy — but prefer reuse.

## Done when
SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN settings; ./build_run.sh Sprout-Claude -seedDemoData YES → Read screenshots/latest.png shows: a paper sheet titled Settings; a REMINDERS section with the reminder-time row carrying a green time chip; a DEVELOPER card with three icon rows, the delete row in red. Tapping 'Delete all plants & rooms' presents the SproutAlert ('Delete everything?') and confirming routes through the existing wipe path. SettingsViewModel tests pass.
