# T002 — Wire bundled fonts into the build + SproutFont helpers

## Overview
Make the committed Bricolage Grotesque / Hanken Grotesk TTFs (already in `Sources/Fonts/`)
load in the app, and give every later task one helper to reach them. Nothing visual changes yet.

## Do
1. Read `Sources/Fonts/FONTS.md` — it lists the 9 TTF files and their EXACT PostScript names.
   Read `.harness/docs/designs/redesign-architecture.md` §2 (SproutFont contract).
2. Ensure the TTFs are bundled: in `project.yml`, confirm (or add) that `Sources/Fonts` is
   included as resources for the `Sprout` target (XcodeGen usually bundles non-source files
   under a sources path automatically — verify by checking the built app after
   `xcodegen generate && xcodebuild ... build`, `ls build/Debug-iphonesimulator/Sprout.app | grep ttf`).
3. Add a `UIAppFonts` array to `Sources/Info.plist` listing all 9 TTF filenames.
4. Create `Sources/DesignSystem/SproutFont.swift` exactly per the architecture contract:
   `SproutFont.display(_:weight:)` (Bricolage; map .semibold→600 file, .bold→700, .heavy/.black→800,
   default .bold), `SproutFont.body(_:weight:)` (Hanken; .regular/.medium/.semibold/.bold),
   `SproutFont.bodyItalic(_:)` (Hanken italic). Use `Font.custom` with the PostScript names from
   FONTS.md verbatim.
5. Add `Tests/SproutFontTests.swift`: for EVERY PostScript name in FONTS.md, assert
   `UIFont(name: <psName>, size: 12) != nil` (this proves registration actually worked).

## Done when
- `xcodebuild test` passes including the new `SproutFontTests` (all 9 fonts resolve).
- `Sources/Info.plist` has `UIAppFonts` with 9 entries; the built .app contains the TTFs.
- `SproutFont.display/body/bodyItalic` exist with the exact signatures from
  redesign-architecture.md §2 and are used nowhere yet (no visual change this task).
