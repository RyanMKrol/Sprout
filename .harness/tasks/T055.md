## Overview
Add a third `crispy` case to the `LeafState` enum so the check-in flow can record dehydrated/under-watered leaves as distinct from `droopy`. This is the additive, persistence-safe foundation the engine (unit B) and UI (unit C) units depend on. It deliberately does NOT change the AdaptiveEngine decision table or the segmented pickers — those are separate units.

## Do
In `Sources/Model/CheckIn.swift`, add `case crispy` to the `LeafState` enum (currently `case fine` / `case droopy` at lines 14-15). LeafState is `String, Codable, CaseIterable, Equatable, Sendable`, so the new case's rawValue is `"crispy"` and the addition is decode-safe — existing stored CheckIns (which only ever hold `fine`/`droopy`) still decode unchanged. Order the cases as the user asked — Crispy, Fine, Droopy — where `allCases` order is observable (declare `case crispy` first, then `fine`, then `droopy`), but note the two UI pickers hard-code their own ordered option arrays (handled in unit C), so this ordering is defensive, not the source of the on-screen order. Confirm `Sources/Persistence/StoredModels.swift` (which stores `LeafState` directly at line 98) needs no change — a new enum case flows through the SwiftData attribute and the `StoredCheckIn` domain mapping without edits. Do NOT edit the AdaptiveEngine, the recommendation presentation, the view models, or the views in this unit; those are units B and C. Leave the two view-model `leaves = .fine` defaults unchanged.

## Done when
`LeafState` has three cases in the order crispy, fine, droopy; the project builds with the new case present (`xcodebuild test` compiles — note the engine's `decide()` switch over soil is exhaustive on SoilMoisture and does NOT switch exhaustively on LeafState, so adding a LeafState case does not by itself force an engine compile error); existing persistence round-trips are unaffected (the ModelTests / PersistenceTests suites still pass); and no engine/presentation/UI files were touched. Persistence-safety is the key property: an added String-rawValue case must not break decoding of previously-stored `fine`/`droopy` check-ins.
