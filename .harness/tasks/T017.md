# T017 — My Plants list restyle

## Overview
The plant list in the new language: white row cards with tokens, italic Latin names, cadence
meta with adaptation suffix, due chips, empty state, swipe actions, green FAB.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screens 04/05 and
   `Sources/Views/PlantListView.swift` + `Sources/ViewModels/PlantListViewModel.swift` first.
2. Restyle: paper background; custom back "‹ Home" (chevronLeft + label, brandGreen body(17,
   .medium)); title "My Plants" display(32); `SproutFAB` top-right → add flow (as today's +).
   Rows per spec §2.12: `.sproutCard()` radius 20, `PlantToken` 46 (photo wins), name
   display(18), species bodyItalic(12.5) #7C8173, meta body(11.5,.semibold) taupe = "Every {N}d"
   + " · shortened"/" · stretched" when the effective interval sits below/above the species
   base (derive from existing view-model why/summary data), trailing `DueChip`. List = ScrollView
   + LazyVStack gap 11, gutter 18 (NOT List) — reimplement swipe with `.swipeActions` only if
   staying on List; otherwise keep List but restyle rows via listRowBackground(paper) +
   listRowSeparator(.hidden) — pick whichever preserves existing swipe Edit/Delete behavior with
   Edit #5E8CA8 / Delete #C4553B.
3. Empty state per screen 05: 84Ø green token ph-flower, "No plants yet" display(22), body copy
   verbatim, primary button "Add your first plant".
4. Delete swipe now presents `SproutAlert` ("Delete {name}?" — copy from spec screen 09) before
   deleting. Keep all deep links (`add`,`basket`,`addflow`,`edit`,`camera`,`photoprompt`) and
   accessibility identifiers working.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN plants;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: paper bg,
  display-font title, white token rows with italic species + colored due chips, green FAB.
- PlantListViewModel tests still green; deleting via swipe shows the confirm dialog first.
