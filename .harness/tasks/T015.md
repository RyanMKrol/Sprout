# T015 — Home hero content component (due / empty / all-watered)

## Overview
The hero card's three content states as a reusable component with testable copy, ahead of the
Home assembly task.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screens 02, 03, 03b (hero portions only).
2. Create `Sources/Views/HomeComponents.swift`:
   - `enum HomeHeroState { case due(count: Int, plants: [Plant]), empty, allWatered(next: (name: String, days: Int)?) }`
   - `HomeHeroCard(state:onPrimaryTap:)` built on `HeroCard`:
     • due: watermark droplet topTrailing; eyebrow "TO WATER TODAY 💧"; `display(60)` count +
       "plants need\nyour care" body(15) white (singular: "plant needs\nyour care"); avatar stack
       of up to 4 due plants (`PlantToken` 36, 2.5 border #285A40, −12 overlap) + names line
       bodyItalic(13.5) white@0.8 ("Basil & Willow" for 2, "Basil, Willow & 3 more" beyond 3);
       cream button "Water these two" (two/three/N wording: 1→"Water it", 2→"Water these two",
       3→"Water these three", else "Water all {N}") + arrowRight.
     • empty: watermark seedling bottomTrailing; "Your garden's\nempty — for now." display(24)
       white; body white@0.82 "Add the plants you own and Sprout keeps their watering on track.";
       cream button "Add your first plant" with plus icon.
     • allWatered: watermark circleCheck topTrailing; eyebrow "ALL DONE FOR TODAY 🌿"; 60Ø
       success PlantToken-style circle with white check; "Nothing needs water." display(24)
       white; if next != nil, bodyItalic(13.5) white@0.8 "Next up: {name} in {days} days."
       ("1 day" singular); NO button.
   - `static func greeting(for state:, hour: Int) -> (eyebrow: String, headline: String)` pure:
     eyebrow "GOOD MORNING ☀️" (5–11) / "GOOD AFTERNOON 🌤" (12–17) / "GOOD EVENING 🌙" (else);
     empty state eyebrow "WELCOME 🌱" + "Let's grow\nsomething."; due → "Two plants are\nready
     for a drink." (spelled counts one…nine, else digits; singular "One plant is…"); allWatered →
     "Everything's\nwatered."
3. Add `Tests/HomeHeroTests.swift` pinning: greeting eyebrow at hours 9/14/21; the due-2
   headline verbatim; button labels for counts 1, 2 and 5; allWatered "Next up" line singular/plural.

## Done when
- `xcodebuild test` passes including `HomeHeroTests` (assertions above verbatim).
- Component + `#Preview` for all three states exist; HomeView itself NOT yet rewired.
