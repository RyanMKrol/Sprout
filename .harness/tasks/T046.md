## Overview
Third attempt at the Edit Plant form redesign. Originally T020 (blocked: ContentView.swift out of scope), re-attempted as T042 which added ContentView.swift but blocked AGAIN — the SECOND root cause was a DIFFERENT out-of-scope file: wiring the 'Change photo' camera flow / new chrome caused the builder to touch Sources/Info.plist (the NSCameraUsageDescription declaration), which T042's scope omitted, so the loop blocked on the first out-of-scope edit. This re-attempt PRESERVES the T020 fix (ContentView.swift in scope) AND adds Sources/Info.plist + project.yml (the XcodeGen source of the plist) to scope — mirroring sibling redesign task T014, whose scope already includes Sources/Info.plist for the same reason.

## Do
Restyle the Edit Plant form (Sources/Views/PlantEditView.swift) into the new redesign chrome per .harness/docs/designs/redesign-spec.md §3 screens 08/09. Read that spec and the existing PlantEditView.swift + PlantEditViewModel.swift FIRST. REUSE the EXISTING design-system components (SproutSheetHeader / SheetScaffold, SproutAlert, PlantToken — all under Sources/DesignSystem, built by T005/T010) — present them, do NOT rebuild, restyle, or edit anything under Sources/DesignSystem. The icon picker (IconPickerView, T021) already exists — PRESENT it via its plant:repository: initializer, don't build it. Concretely: (1) Header via SproutSheetHeader/SheetScaffold: Cancel / "Edit Plant" / Save on paper. (2) PHOTO section (eyebrow): white row with 64Ø PlantToken (photo wins) + "Change photo" green body(17,.medium) -> existing single-shot camera cover. (3) ICON section: white row with current glyph in a 34Ø token + "Change icon" green -> present IconPickerView(plant:repository:) for this plant. (4) NICKNAME section: white body(17) field. (5) ROOM section: white row with the existing Menu room picker. No species field (locked). (6) Bottom (30 gap): white radius-18 row, border rgba(196,85,59,0.2), trash icon + "Delete Plant" body(17,.semibold) #C4553B -> SproutAlert titled "Delete {name}?" / body "This removes the plant and its check-in history. This can't be undone." / Cancel / Delete(#C4553B); confirm deletes via the existing view-model/repository path and dismisses to the list. (7) Keep the `edit` deep-link auto-open (SPROUT_SCREEN=edit, handled in PlantListView.deepLinkIfRequested) and existing accessibility identifiers. Wire the icon-picker presentation and any needed plant/repository handoff through the presenter (ContentView.makeEditor) and/or the view model — ContentView.swift IS in scope. IMPORTANT (second lesson): the camera permission string NSCameraUsageDescription already exists in BOTH project.yml (line ~45, the XcodeGen source of truth) and the generated Sources/Info.plist — do NOT duplicate or re-add it; both files are now in scope ONLY so that any incidental camera-permission touch while wiring the 'Change photo' flow does not trip the structural scope gate. Prefer NOT to touch Info.plist / project.yml at all unless the camera flow genuinely requires a new usage-description key. Do NOT edit Sources/DesignSystem.

## Done when
Launching with SPROUT_SCREEN=edit and seeded demo data (SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN edit; ./build_run.sh Sprout-Claude -seedDemoData YES) and reading screenshots/latest.png shows the new SproutSheetHeader chrome with PHOTO / ICON / NICKNAME / ROOM sections and a red "Delete Plant" row. Tapping "Change icon" presents the existing IconPickerView for the plant. Delete goes through the SproutAlert confirm dialog and, on confirm, removes the plant via the existing repository path and dismisses to the list. PlantEditViewModel tests stay green; no edits under Sources/DesignSystem; NSCameraUsageDescription is not duplicated in Sources/Info.plist or project.yml.
