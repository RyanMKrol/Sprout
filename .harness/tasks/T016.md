# T016 — Home screen assembly (Botanical Editorial)

## Overview
Rebuild HomeView's body in the new language: logo lockup top bar, greeting, reminders-off
banner, the hero (replacing the Water tile), sage/oat bento, ghost-button row — preserving
every deep link and existing behavior.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screens 02/03/03b and `Sources/Views/HomeView.swift`
   fully first — all sheet/cover presentation and the SPROUT_SCREEN deep-link switch MUST keep
   working; `Tests/HomeTileTextTests.swift` shows which text helpers exist.
2. Replace HomeView's visual layer: background SproutTheme.paper; top bar = logo lockup (34×34
   radius-11 logoGradient tile + seedling + "Sprout" display(22)) left, right
   `CircularToolbarButton`s — bell-slash #C4663F (only when reminders off) + gear #4A5142
   (opens Settings as today). Greeting eyebrow+headline from `HomeHeroCard.greeting`. Banner:
   restyled reminders-off row (white radius-18, terracotta border/icon per spec; same tap
   behavior). `HomeHeroCard` wired: state from PlantListViewModel due data (due plants → .due;
   no plants → .empty; else .allWatered with soonest-scheduled plant); primary tap → guided
   watering (.due mode) / add flow. Bento row: sage My Plants (PlantToken stack of up to 3
   plants, 38, sage border; "{N} growing" / "None yet") → plants list; oat Rooms (house bubble;
   "{N} spaces" / "Set one up") → rooms. Ghost row: "+ Add a plant" and list-check "Check in"
   (GhostButtonStyle) → add sheet / full check-in cover.
3. Move now-dead tile structs (HomeTileStyle, TileIcon, HomeSquareTile, HomeWideTile,
   HomeActionTile, TileBackground) OUT of the body path but LEAVE the types in place (deleted in
   the cleanup task). Keep NotificationsOffBanner logic, badges and the pulsing behavior notion
   dropped (hero replaces it). Keep every accessibilityIdentifier currently present.
4. Update `HomeTileText` helpers only if they feed the new copy; keep their tests green
   (adjust tests ONLY where copy legitimately changed to the §3 strings).

## Done when
- All SPROUT_SCREEN deep links behave as before (spot-check: `water`, `settings`, `rooms`).
- `SIM=$(./tools/loop_sim.sh); ./build_run.sh Sprout-Claude -seedDemoData YES` → Read
  `screenshots/latest.png`: paper background, logo lockup, greeting matching seeded due count,
  green hero with numeral + avatar stack + cream button, sage+oat tiles, two ghost buttons —
  and NO old gradient tiles.
- Full test suite passes.
