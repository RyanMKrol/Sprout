# T021 — Icon picker sheet (NEW)

## Overview
The "Choose an icon" sheet: a 4-column grid of the 16 Phosphor plant glyphs with live token
preview, persisting the choice to the plant.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 11b. Plant.icon (T012) and
   PlantToken (T005) exist — consume them.
2. Create `Sources/Views/IconPickerView.swift`: init with a plant (or binding + onSave) and the
   repository seam used by other edit flows. `SproutSheetHeader` (Cancel / "Choose an icon" /
   Done) on paper. Preview: 88Ø `PlantToken` with the currently-highlighted icon + name
   display(20) + species bodyItalic. "PICK AN ICON" eyebrow; LazyVGrid 4 columns gap 12: square
   radius-18 cells — selected: brandGreen fill, white glyph, shadow 0 8 18 rgba(47,107,76,0.34);
   idle: white, hairline border rgba(34,39,31,0.06), glyph #3A4136 28pt. All 16 `PlantIcon`
   cases in order. Footer hint verbatim: "Set the look now — you can change it anytime from the
   plant's page." Done persists via the repository update path.
3. Give each cell `.accessibilityIdentifier("icon.\(rawValue)")`.

## Done when
- Presented from a `#Preview` and from Edit Plant (wired in T020 — if T020 not yet merged, just
  make the view presentable standalone). Selection round-trips: pick a different icon, Done,
  reopen → the new icon is highlighted (assert the repository update in a small unit test if a
  seam exists, else document manual verification in the worklog).
- Build + full suite green.
