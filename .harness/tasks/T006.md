# T006 — DueChip + DueStatus

## Overview
The small colored due-status chip used on plant rows and room-detail rows.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §1.2 (semantic colors) + §2.2 and
   `.harness/docs/designs/redesign-architecture.md` §2 (DueChip contract).
2. Create `Sources/DesignSystem/DueChip.swift`: `enum DueStatus { case overdue(days: Int),
   dueToday, due(inDays: Int), unscheduled }` with `var label: String` — "Overdue 1d"/"Overdue Nd",
   "Due today", "Due in Nd", "No schedule" — and `var tint/background: Color`
   (overdue #C4553B on rgba(196,85,59,0.13); today #B4832F on rgba(198,138,46,0.16); due
   #2F6B4C on rgba(47,107,76,0.13); unscheduled SproutTheme.textTertiary on
   rgba(60,66,58,0.08)). `DueChip(status:)` renders label in SproutFont.body(11,.bold), padding
   5×11, capsule-ish RoundedRectangle radius 10.
3. Add `Tests/DueChipTests.swift` asserting the four labels above (including day interpolation).

## Done when
- `xcodebuild test` passes including `DueChipTests`; labels match exactly.
