## Overview
Third attempt at the guided-watering 'All done 🌿' completion screen — originally T034, then T043. T034 blocked for touching the coordinator + ContentView (which the mode-aware body genuinely needs). T043 correctly added those two files to scope BUT blocked AGAIN for an unforced, spec-irrelevant out-of-scope edit to Sources/Info.plist — a file the work never requires. This re-attempt keeps the (now-correct) coordinator + ContentView scope, narrows Sources/Views/** to just GuidedWateringView.swift, and forbids Info.plist explicitly so the wander cannot recur.

## Do
Preserve the T043 fix (coordinator + ContentView are IN scope for a mode pass-through). Do NOT touch Sources/Info.plist, project.yml, assets, or any launch-screen config — none of this work needs them; editing them is what blocked T043. Steps: 1) Add a stored `let mode: Mode` to `GuidedWateringCoordinator` (init parameter, default `.all`); set it from the initializer only — do not change how the plant set is selected or any other coordinator behaviour. 2) In `ContentView.makeGuidedWatering(_ mode:)`, pass the existing `mode` value into the `GuidedWateringCoordinator(...)` init (currently it is discarded after picking the plant set). 3) Add a pure `static func completionBody(for mode: Mode) -> String` on `GuidedWateringCoordinator` returning `.due` → "You've been through every plant that needed water today." and `.all` → "You've checked in on every plant." 4) In `completionStep`, render the body via that helper using the coordinator's mode instead of the hardcoded due-mode string at line ~271. While in `completionStep`, align the restyle to redesign-spec §3 screen 22: Close-only header; centered 104pt success circle filled with `PlantTokenPalette.success` (currently `SproutTheme.brandGreen`) + white ~46pt check with a pop-in on appear (scale 0.5→1.1→1, `.animation(_, value:)` idiom); "All done 🌿" as `SproutFont.display(27, weight: .bold)`; body as `SproutFont.body(16)` (currently `body(15)`) in `SproutTheme.textMuted` (#6E7A63); a pinned primary "Done" using `SproutPrimaryButtonStyle`. 5) Add/extend a unit test in Tests/** (e.g. Tests/GuidedWateringCoordinatorTests.swift) asserting BOTH exact strings via the pure helper. Keep the `#Preview` rendering both mode bodies (one coordinator per mode).

## Done when
`GuidedWateringCoordinator` has a stored `mode` and a pure `completionBody(for:)`; `ContentView.makeGuidedWatering` passes the mode into the init; `completionStep` renders the mode-appropriate body via the helper (no hardcoded single string) and matches screen 22 (Close-only header, 104pt success-duo circle + white check with pop-in, display(27) title, body(16) muted-green, pinned primary Done). A unit test asserts `completionBody(for: .due)` == "You've been through every plant that needed water today." and `completionBody(for: .all)` == "You've checked in on every plant." and passes. The diff touches ONLY the four scoped files — in particular Sources/Info.plist is unchanged.
