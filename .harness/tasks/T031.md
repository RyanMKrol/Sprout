# T031 — Check-in input sheet restyle

## Overview
The check-in form (soil / leaves / watered toggle) in the new sheet language.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 18 and `Sources/Views/CheckInView.swift`
   + `Sources/ViewModels/CheckInViewModel.swift` first.
2. Restyle the input phase: full sheet on paper, `SproutSheetHeader` (Cancel / "Check in" /
   no confirm). Header: `PlantToken` 60 + name display(21) + bodyItalic species. "SOIL"
   eyebrow + `SproutSegmentedPicker` Dry/Moist/Wet; "LEAVES" + Fine/Droopy. Toggle row white
   r16: "I watered it" body(17) + Toggle (tint brandGreen; off-track approximates #D6D0C0).
   Helper verbatim: "Sprout learns each plant's true rhythm from these quick reads and adjusts
   its schedule." Pinned `PrimaryButton` "Save check-in".
3. The result phase is T032 — do not restyle it here. Keep the `checkin` deep link + identifiers.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN checkin;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: token +
  display name header, two green-pill segmented groups, watered toggle row, pinned CTA.
- CheckInViewModel tests green.
