# T027 — Rooms list restyle + room delete confirmation

## Overview
The Rooms list with per-room icons, "N plants" counts, swipe-to-delete with the new confirm —
and tap now navigates to the room DETAIL (T028), not the editor.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screens 14/15b and `Sources/Views/RoomsView.swift`
   + `Sources/ViewModels/RoomsViewModel.swift` first. RoomDetailView (T028) exists — navigate to it.
2. Restyle: paper; back "‹ Home"; title "Rooms" display(32); `SproutFAB` → add-room sheet.
   Rows white radius-20: 44×44 radius-14 oat bubble with per-room `ChromeIcon` — name-keyed
   mapping: contains "living"→couch, "bed"→bed, "bath"→bath, "kitchen"→utensils,
   "dining"→mugSaucer, else house — #B4832F; name display(18); env summary body(12.5);
   trailing "{N} plants"/"1 plant" body(13,.semibold) taupe + chevron. Row tap → push
   `RoomDetailView(room:)`. Editor is no longer reachable from the row (Detail's Edit button
   owns that); keep the `roomeditor`/`addroom` deep links working (roomeditor may now push
   detail then present the editor).
3. Swipe-delete (red trash) → `SproutAlert`: "Delete {room}?" / body per plant count — with
   plants: "Its {N} plants stay in your garden without a room's light and humidity. This can't
   be undone."; zero: "This can't be undone." / Cancel / Delete(#C4553B) → existing delete path.
4. Empty state: keep today's semantics, restyled minimal (eyebrow-less hint text + primary
   "Add a room" button).

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN rooms;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: white rows
  with oat icon bubbles, plant counts, display-font title.
- RoomsViewModel tests green; delete goes through the confirm.
