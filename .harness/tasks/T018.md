# T018 — Plant detail restyle + rhythm band

## Overview
The plant page: big centered token header, the three-zone schedule card with the new
watering-rhythm visualization, restyled history, and the check-in CTA.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 06 and
   `Sources/Views/PlantDetailView.swift` + `Sources/ViewModels/PlantDetailViewModel.swift` first.
2. Expose rhythm inputs on PlantDetailViewModel if not present: `minDays`, `maxDays`, `baseDays`
   (species base interval), `effectiveDays` (current interval) — from the care profile /
   schedule data it already loads. Add unit tests pinning them for a fixture plant.
3. Restyle the screen: paper bg; back "‹ Plants"; "Edit" text button (green body(17,.semibold)).
   Header: `PlantToken` 112 (photo wins), name display(32), species bodyItalic(16) #7C8173.
   Schedule card (`.sproutCard()` radius 20, three zones with inset dividers): (1) 34×34
   radius-10 soft-green droplet bubble + due line display(17) in due-status color + "Tap to
   adjust when it's next due" body(12) #9AA090 + trailing pencil — whole row opens the adjust
   sheet (as today); (2) `RhythmBand(minDays:maxDays:baseDays:effectiveDays:)`; (3) the why
   sentence body(13) #6E7A63. CTA: primary button "✓ Check in on {name}" → check-in sheet.
   "CHECK-IN HISTORY" eyebrow + white card rows: date body(15,.semibold) + "Soil dry · Leaves
   fine" body(12.5) + trailing "💧 Watered" body(12.5,.bold) green when watered.
4. Keep the `checkin` deep-link auto-sheet and all identifiers.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN detail;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: centered
  112 token, display name, schedule card with visible rhythm track (thumb + base marker +
  MIN/MAX labels), history rows.
- New view-model tests for min/base/effective pass; existing PlantDetailViewModel tests green.
