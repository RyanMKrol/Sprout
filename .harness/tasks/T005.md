# T005 — PlantToken view + PlantTokenPalette

## Overview
The circular gradient plant token — the redesign's identity element — with the photo-wins rule
and deterministic per-plant gradient assignment (replacing the old PlantPalette look, which
stays untouched until the cleanup task).

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §1.2 (token gradients) + §2.1 and
   `.harness/docs/designs/redesign-architecture.md` §2 (PlantToken/PlantTokenPalette contract).
2. Create `Sources/DesignSystem/PlantToken.swift`:
   - `PlantTokenPalette` with the 6 duos (green #79B98C→#3F7E58, purple #A98CC0→#6E4E8C, blue
     #8CB0D2→#4E7CA6, gold #DCC078→#B08A34, teal #5FB4A2→#2E7C6B, pink #D79BB0→#B05F7E) +
     `success` (#79B98C→#2E7C4E); `duo(for id: UUID)` deterministic — hash the uuidString the same
     way `PlantPalette.color(for:)` in `Sources/Support/DisplayFormatting.swift` does, mod 6.
   - `PlantToken: View` per the contract: `init(icon: PlantIcon, duo: Duo, size: CGFloat,
     photo: UIImage? = nil)`. photo != nil → `Image(uiImage:)` scaledToFill clipped to Circle.
     Else Circle filled with `RadialGradient` (130% radius, center offset to 30%/25% — approximate
     with `startRadius 0, endRadius size*0.75, center UnitPoint(x:0.3,y:0.25)`), white glyph at
     `size*0.45`, shadow color = duo.dark at 0.28 opacity, radius 5, y 2.
3. Add `Tests/PlantTokenTests.swift`: `duo(for:)` is stable (same UUID → same duo, two calls) and
   two fixed distinct UUID literals map to the duos your hash gives them (pin the actual values).

## Done when
- `xcodebuild test` passes including `PlantTokenTests`.
- `PlantToken` renders (add a lightweight `#Preview` block) and no existing screen uses it yet.
