# T023 — Add flow: basket step restyle

## Overview
Step 2 (basket + species search) in the new language, with token-tap opening the icon picker.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 11 and the basket-step section of
   `Sources/Views/AddFlowView.swift` + `Sources/ViewModels/BasketAddViewModel.swift`.
2. Restyle: header Cancel / "Add Plants". Room summary row (white r16: house bubble + room name
   body(16,.medium) + trailing "Change" green body(15,.semibold)) → back to room step.
   "BASKET · {N}" eyebrow; compact rows white r15: `PlantToken` 34 + nickname body(15,.semibold)
   + species bodyItalic(12) + trailing green shuffle icon (re-roll nickname, as today). Tapping
   a basket row's TOKEN presents `IconPickerView` for that basket entry (persist icon into the
   pending entry so it's saved on commit). "ADD SPECIES" eyebrow; search field
   rgba(120,120,110,0.1) r13 with magnifyingGlass; result rows white r14 with name + trailing
   green circlePlus. Pinned bottom `PrimaryButton` "Add {N} Plants" ("Add 1 Plant" singular).
3. Keep search/commit/photo-prompt chaining and the `basket` deep link; keep identifiers.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN basket;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: basket rows
  with 34 tokens + shuffle, search field, pinned green CTA with count.
- BasketAddViewModel tests green (extend for icon-carrying pending entries if the type changes).
