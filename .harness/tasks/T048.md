## Overview
Third attempt at the Settings restyle: T035 blocked on scope-creep into Sources/DesignSystem/SproutFont.swift; T044 (which widened scope to DesignSystem + a reuse directive) then blocked AGAIN on a NEW off-scope excursion into Sources/Info.plist — the agent evidently went plumbing the 'Send a test reminder' notification path instead of doing a pure visual restyle. This attempt keeps T044's DesignSystem-in-scope + reuse fix and adds an explicit fence: restyle presentation ONLY, never touch Info.plist or the test-reminder / notification wiring.

## Do
Restyle Sources/Views/SettingsView.swift into the redesign visual language per redesign-spec.md §3 screen 24, REUSING existing DesignSystem primitives — do NOT add new font helpers or a new alert component; the ones you need already exist (SproutAlert + .sproutAlert in Sources/DesignSystem/AlertCard.swift, SproutFont.body(_:weight:) incl. .semibold, and the eyebrow pattern via .tracking().textCase(.uppercase) as used in AddRoomView.swift). HARD FENCE: this is a VISUAL restyle only. Do NOT edit Sources/Info.plist, do NOT add/alter notification or background-mode declarations, and do NOT change the notification / test-reminder wiring, the deleteAllData() wipe path, or SettingsViewModel behaviour — leave every capability exactly as it works today and only re-skin the presentation. If a change seems to require editing Info.plist or notification plumbing, that is a sign you are out of scope: stop and re-skin only. Concretely: 1) Read redesign-spec.md §3 screen 24, SettingsView.swift, SettingsViewModel.swift, and the DesignSystem primitives (AlertCard.swift, SproutFont.swift) and how AddRoomView.swift renders eyebrow labels. 2) Sheet layout on paper: top spacer / 'Settings' title / 'Done' button. 'REMINDERS' eyebrow; white r16 row 'Reminder time' in body(17) with a trailing soft-green chip (rgba(47,107,76,0.1) r10) showing the time in green body(17,.semibold); the row opens the existing DatePicker (keep the current inline/sheet mechanism, restyled). Helper text verbatim: 'Watering reminders arrive at this time on the day a plant is due — pick a window you're usually home.' 3) 'DEVELOPER' eyebrow; white card rows with leading icons: 'Camera diagnostics' (doc.text/fileLines icon, chevron, navigates to DiagnosticsView), 'Send a test reminder (5s)' (bell) — keep its existing action, only re-skin — and 'Delete all plants & rooms' (trash, color #C4553B). Helper text verbatim: 'Diagnostics capture camera issues. The test reminder fires in 5 seconds. Deleting is permanent.' 4) Replace the current confirmationDialog on 'Delete all plants & rooms' with the existing SproutAlert component: title 'Delete everything?', message 'Every plant, room and check-in will be removed. This can't be undone.', Cancel + Delete(#C4553B) confirm wired to the existing deleteAllData() wipe path. 5) Preserve the existing 'settings' deep link and accessibility identifiers. If (and only if) a DesignSystem primitive is genuinely missing, extend it minimally in Sources/DesignSystem/** rather than inlining a bespoke copy — but prefer reuse.

## Done when
SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN settings; ./build_run.sh Sprout-Claude -seedDemoData YES → Read screenshots/latest.png shows: a paper sheet titled Settings; a REMINDERS section with the reminder-time row carrying a green time chip; a DEVELOPER card with three icon rows, the delete row in red. Tapping 'Delete all plants & rooms' presents the SproutAlert ('Delete everything?') and confirming routes through the existing deleteAllData() wipe path. SettingsViewModel tests pass. The diff touches ONLY Sources/Views/SettingsView.swift (+ Sources/DesignSystem/** and Tests/** if strictly needed) — Sources/Info.plist is NOT modified.
