## Overview
Re-attempt of T028: the per-room detail page (brightness/humidity stat duo, a plain-language watering-impact banner, and the room's plants), closing the old "rooms feel dead-end" flag. The first attempt was BLOCKED at rung 0 (haiku) for a single out-of-scope edit to Sources/ContentView.swift — the loop blocks on the FIRST out-of-scope file. All the wiring this task actually needs (the Route enum, .navigationDestination, and the SPROUT_SCREEN deep-link switch) lives in Sources/Views/HomeView.swift, which was already in scope, so the ContentView edit was gratuitous; this re-attempt (a) adds Sources/ContentView.swift to scope defensively — the facets vocabulary itself groups ContentView under the `views` layer, so a model may reach for it — and (b) starts above rung 0 for tighter scope discipline.

## Do
1. Read redesign-spec.md §3 screen 15 (incl. the ⚑ derived copy formula), Sources/Engine/RoomEnvironment.swift (the environment factor; READ-ONLY — do not edit it, it is out of scope), Sources/ViewModels/RoomsViewModel.swift, and Sources/Views/RoomsView.swift (its row-tap + editor sheet).
2. Create a pure helper `static func impactLine(factor: Double) -> String` (in Sources/Views/RoomDetailView.swift or Sources/Support/): factor > 1 → "{Lead} dries soil faster — plants here are watered about {P}% more often."; factor < 1 → "{Lead} … watered about {P}% less often."; factor == 1 → "Balanced light and humidity — no adjustment to watering here." P = round(|1−factor|×100); lead word from the room's brightness ("Bright light" / "Low light"). Add Tests/RoomDetailTests.swift pinning 1.15 → "15% more often", 0.9 → "10% less often", 1.0 → the balanced line (verbatim).
3. Create Sources/Views/RoomDetailView.swift per spec: paper bg; "‹ Rooms" back; "Edit" text button → the existing RoomEditorView sheet; room-name title display(32); stat duo — oat card (sun #D98B0A, "Brightness" body(12,.semibold) #8A7A55, value display(19)) + sage card (droplet green, "Humidity", value); InfoBanner(arrowTrendUp, impactLine); "PLANTS IN THIS ROOM" eyebrow; compact white rows (PlantToken 40 + name + bodyItalic species + small DueChip) → NavigationLink to plant detail; empty state hint "No plants in this room yet."
4. Wire navigation in Sources/Views/HomeView.swift: add a Route case for room detail and its .navigationDestination, and route the `roomdetail` SPROUT_SCREEN case in HomeView.deepLinkIfRequested() to push Rooms then the first room's detail. Also make RoomsView rows navigate to room detail (spec: "Tap → room detail (15), NOT the editor"). Keep every edit inside the declared scope — do NOT touch Sources/Engine/** or any file not listed.

## Done when
SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN roomdetail; ./build_run.sh Sprout-Claude -seedDemoData YES → screenshots/latest.png shows the room-detail screen: the Brightness/Humidity stat duo, the green watering-impact banner, and plant rows with due chips. RoomDetailTests pass with the three verbatim pinned strings. No file outside the declared scope is modified.
