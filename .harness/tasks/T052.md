## Overview
Both the My Plants list and the plant detail screen are pushed onto HomeView's NavigationStack and each shows TWO back affordances at once, one of them non-functional. On My Plants (Sources/Views/PlantListView.swift) an in-content custom '‹ Home' Button (~lines 66-79) whose action is an EMPTY stub `/* dismiss or navigate back */` (line 68) renders alongside the working system nav back button. On plant detail (Sources/Views/PlantDetailView.swift) a leading toolbar item is a decorative `Label("Plants", systemImage: "chevron.left")` (~lines 70-75) — a Label, not a Button, so it does nothing — rendering alongside the working system back button. Neither view sets .navigationBarBackButtonHidden. RoomDetailView (Sources/Views/RoomDetailView.swift, lines 20/60/72-80) already establishes the correct, working pattern to align to.

## Do
Standardise both screens on RoomDetailView's pattern: exactly ONE functional custom back control (a Button calling @Environment(\.dismiss)'s dismiss()) plus `.navigationBarBackButtonHidden(true)` so the system back button is hidden and does not duplicate it. (1) PlantDetailView: add `@Environment(\.dismiss) private var dismiss`, add `.navigationBarBackButtonHidden(true)` alongside the existing navigationTitle modifiers, and convert the leading toolbar Label at ~line 71 into a `Button { dismiss() } label: { Label("Plants", systemImage: "chevron.left").labelStyle(.iconOnly) }` keeping the brandGreen tint / 17pt-semibold styling and adding `.accessibilityLabel("Plants")` — mirroring RoomDetailView lines 72-80. (2) PlantListView: add `@Environment(\.dismiss) private var dismiss`, wire the existing custom '‹ Home' Button's empty action (line 68) to call `dismiss()` (keep its ChromeIcon.chevronLeft + "Home" label, which is already correctly sized with .resizable().frame), and add `.navigationBarBackButtonHidden(true)` to the view so the system back chevron no longer appears next to it. Consult the axiom-swiftui skill for the correct iOS 18/26 NavigationStack back-button pattern before implementing. Preserve the swipe-back gesture (navigationBarBackButtonHidden keeps interactive-pop working in a NavigationStack).

## Done when
swiftlint passes and `xcodegen generate && xcodebuild test` builds/passes. Empirical: run `./build_run.sh Sprout-Claude -seedDemoData YES` with `SPROUT_SCREEN=plants` (My Plants list) — Read screenshots/latest.png and confirm exactly ONE back control top-left (the '‹ Home' button), no duplicate system chevron beside it. Then repeat with `SPROUT_SCREEN=detail` (plant detail) — confirm exactly ONE back chevron top-left, no duplicate. Manually/logically confirm the surviving back control on each screen actually pops the stack (it calls dismiss()), not a no-op. Follow CLAUDE.md screenshot-lore if a shot looks stale.
