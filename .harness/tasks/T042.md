## Overview
Re-attempt of T020, which was BLOCKED for scope-creep: the attempt edited Sources/ContentView.swift (the presenter that builds the editor via makeEditor and owns the shared repository/care-database), but ContentView.swift was omitted from the scope. Wiring the T021 icon picker (whose plant/repository initializer needs the plant + PlantRepository) into the edit form legitimately requires touching that presenter, so the scope now includes Sources/ContentView.swift — exactly as sibling redesign task T016 (Home screen assembly) already did.

## Do
Restyle the Edit Plant form (Sources/Views/PlantEditView.swift) into the new redesign chrome, per .harness/docs/designs/redesign-spec.md §3 screens 08/09. Read that spec and the existing PlantEditView.swift + PlantEditViewModel.swift first. Use the EXISTING design-system components (SproutSheetHeader / SheetScaffold, SproutAlert, PlantToken — all in Sources/DesignSystem, already built by T005/T010) — present them, do not rebuild or restyle them. The icon picker (IconPickerView, T021) already exists — PRESENT it for this plant via its plant:repository: initializer, don't build it. Concretely: (1) Header via SproutSheetHeader/SheetScaffold: Cancel / "Edit Plant" / Save on paper. (2) PHOTO section (eyebrow): white row with 64Ø PlantToken (photo wins) + "Change photo" green body(17,.medium) -> existing single-shot camera cover. (3) ICON section: white row with current glyph in a 34Ø token + "Change icon" green -> present IconPickerView(plant:repository:) for this plant. (4) NICKNAME section: white body(17) field. (5) ROOM section: white row with the existing Menu room picker. No species field. (6) Bottom (30 gap): white radius-18 row, border rgba(196,85,59,0.2), trash icon + "Delete Plant" body(17,.semibold) #C4553B -> SproutAlert titled "Delete {name}?" / body "This removes the plant and its check-in history. This can't be undone." / Cancel / Delete(#C4553B); confirm deletes via the existing view-model/repository path and dismisses to the list. (7) Keep the `edit` deep-link auto-open (SPROUT_SCREEN=edit, handled in PlantListView.deepLinkIfRequested) and existing accessibility identifiers. Wire the icon-picker presentation and any needed plant/repository handoff through the presenter (ContentView.makeEditor) and/or the view model as needed — ContentView.swift is now in scope, so surface whatever the picker needs (the plant + PlantRepository) rather than reaching around it. Do NOT edit Sources/DesignSystem — reuse those components as-is.

## Done when
Launching with SPROUT_SCREEN=edit and seeded demo data (SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN edit; ./build_run.sh Sprout-Claude -seedDemoData YES) and reading screenshots/latest.png shows the new sheet chrome with PHOTO / ICON / NICKNAME / ROOM sections and a red "Delete Plant" row. Tapping "Change icon" presents the existing IconPickerView for the plant. Delete goes through the SproutAlert confirm dialog and, on confirm, removes the plant via the existing repository path and dismisses to the list. PlantEditViewModel tests stay green; no edits under Sources/DesignSystem.
