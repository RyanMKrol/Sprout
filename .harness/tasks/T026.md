# T026 — Single-shot camera restyle

## Overview
The one-photo camera cover (from Edit Plant) in the same dark language.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 26 and `Sources/Views/CapturePhotoView.swift`.
2. Restyle: background #10160E; title "Take a photo" display(22) white; square radius-26
   viewfinder (live preview or the simulator placeholder as today); bottom "Cancel" body(17)
   white left + the same 84/70 white shutter as T025.
3. Behavior unchanged (hands the image back to the edit form).

## Done when
- Build green; `#Preview` shows the layout; existing photo-edit flow still works (verified via
  the `edit` screenshot showing no regression after tapping is not scriptable — code review +
  preview suffice; note it in the worklog).
