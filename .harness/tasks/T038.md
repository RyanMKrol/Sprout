# T038 — Redesign cleanup sweep: delete dead styling

## Overview
With every screen re-skinned, remove the orphaned old design system so the codebase has one
visual language.

## Do
1. Confirm via grep that these are now unreferenced, then DELETE: `HomeTileStyle`, `TileIcon`,
   `TileBackground`, `HomeSquareTile`, `HomeWideTile`, `HomeActionTile` (HomeView/
   HomeComponents), the old `PlantPalette` 10-hue implementation in
   `Sources/Support/DisplayFormatting.swift` (keep the file's other helpers; PlantTokenPalette
   replaced it), the old `PlantThumbnail`/`PlantCardView` remnants in PlantListView if the
   restyle left duplicates, and any now-unused inline `Color(red:green:blue:)` literals in
   redesigned files (grep `Color(red:` under Sources/Views and justify each survivor).
2. Update `Tests/**` that referenced deleted symbols. Remove dead SF Symbol references in
   redesigned screens (they should all use ChromeIcon now — grep `Image(systemName:` under
   Sources/Views and convert stragglers ONLY in already-redesigned files).
3. Add a row to `.harness/custom/docs/LIMITATIONS.md`: redesigned screens use fixed custom
   font sizes (no Dynamic Type scaling this wave) — why, impact, revisit trigger.

## Done when
- `grep -rn "HomeTileStyle\|PlantPalette" Sources Tests` returns nothing (PlantTokenPalette
  excepted); full suite green; `./build_run.sh Sprout-Claude -seedDemoData YES` screenshot
  shows Home unchanged from T016's result.
