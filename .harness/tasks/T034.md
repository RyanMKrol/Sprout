# T034 — Guided watering: completion screen restyle

## Overview
The "All done 🌿" end state of guided watering.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 22 and the completion section of
   `Sources/Views/GuidedWateringView.swift`.
2. Restyle: header "Close" only. Centered: 104Ø success-duo circle (PlantTokenPalette.success)
   with white check 46 and a pop-in (scale 0.5→1.1→1 keyed animation on appear); "All done 🌿"
   display(27); body(16) #6E7A63 — due mode: "You've been through every plant that needed water
   today."; all-plants mode: "You've checked in on every plant." Pinned `PrimaryButton` "Done".
3. Coordinator untouched.

## Done when
- Build + coordinator tests green; `#Preview` renders both mode bodies (assert the two body
  strings via a small pure helper + unit test if one exists, else pin them in a new tiny test).
