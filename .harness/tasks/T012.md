# T012 — Plant.icon on the model (schema + repository + demo seed)

## Overview
Persist each plant's chosen Phosphor icon: new optional SwiftData field surfaced as a typed
`PlantIcon`, defaulted from the species, carried through the repository, and varied in demo data.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §5 and `.harness/docs/designs/redesign-architecture.md` §3.
   Then read `Sources/Model/` (Plant), `Sources/Persistence/` (StoredPlant + PlantStore mapping)
   to bind to the real property/CRUD names.
2. Add `iconName: String?` to `StoredPlant` (optional → SwiftData lightweight migration; do NOT
   add a VersionedSchema this wave). Map it in every StoredPlant↔Plant conversion.
3. Add `icon: PlantIcon` to `Plant`: stored rawValue decoded with
   `PlantIcon(rawValue:) ?? .default(forSpecies: species)`; new-plant initializers default it to
   `.default(forSpecies:)`. Repository `add`/`update` persist it.
4. In `Sources/DemoSeed.swift`, give the 5 demo plants distinct icons (flower, plant, leaf,
   pottedPlant, cactus).
5. Extend `Tests/PersistenceTests.swift` (or a new test file): a plant saved with `.cactus`
   round-trips through the store with `.cactus`; a StoredPlant with `iconName = nil` or garbage
   surfaces the species default; existing tests stay green.

## Done when
- `xcodebuild test` passes; the round-trip + fallback assertions above exist and pass.
- No UI files changed (model/persistence/demo seed only).
