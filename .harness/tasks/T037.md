# T037 — Notification intro sheet restyle

## Overview
The first-run "Never miss a watering" explainer in the new language.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 01 and
   `Sources/Views/NotificationIntroView.swift` (gatekeeper logic unchanged).
2. Restyle: bottom sheet on paper (`.sproutSheetBackground()`, padding 14/28/44, handle).
   Centered: 112Ø `SproutTheme.logoGradient` circle, shadow 0 16 34 rgba(30,70,50,0.4), white
   bell 46; "Never miss a watering" display(29); body(16) lh1.5 #6E7A63 verbatim: "Sprout sends
   one gentle reminder a day when your plants need water, at a time you choose. 🌱";
   `PrimaryButton` "Enable reminders"; text button "Maybe later" green body(17,.semibold)
   padding-top 18.
3. Keep show-once/gatekeeper semantics and the `notifyoff` demo hook.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN notifyoff;
  ./build_run.sh Sprout-Claude -seedDemoData YES`
  → Read `screenshots/latest.png`: home shows the bell-slash/off state; then verify the intro
  sheet itself via `#Preview` (gradient bell circle, display title, green CTA) and note it in
  the worklog.
- NotificationGatekeeper tests green.
