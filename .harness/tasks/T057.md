## Overview
Surface the new Crispy leaf state in the UI and reword the leaf question. Two changes: (1) COPY — the leaf-question label reads 'How do the leaves look?' at both check-in sites. (2) UI — the leaves segmented picker offers three options in the order Crispy, Fine, Droopy at both sites. Also finalise the two view-model `message` strings for the new crispy reasons. Depends on units A (the LeafState.crispy case) and B (the crispy Reason cases + engine mapping).

## Do
COPY: In `Sources/Views/GuidedWateringView.swift` reportStep, change the `SectionEyebrow(text: "How does it look?")` at line 95 to `"How do the leaves look?"`. In `Sources/Views/CheckInView.swift` the LEAVES section eyebrow currently reads `"Leaves"` (line 101) — per the DoD question, make the leaf question explicit here too (default: change the eyebrow to `"How do the leaves look?"`; if the product answer says keep the 'Leaves' eyebrow and add a supporting line instead, do that). PICKER: In both leaves `SproutSegmentedPicker` option arrays — CheckInView.swift lines 104-110 and GuidedWateringView.swift leavesPicker lines 169-177, each currently listing only `(LeafState.fine, "Fine")` / `(LeafState.droopy, "Droopy")` — add `(LeafState.crispy, "Crispy")` as the FIRST entry so the on-screen order is Crispy, Fine, Droopy. VIEW-MODEL COPY: add the crispy reason arms to the exhaustive `message` switches in `Sources/ViewModels/CheckInViewModel.swift` (Result.message, ~lines 59-80) and `Sources/ViewModels/GuidedWateringCoordinator.swift` (message, ~lines 81-94) — e.g. crispyDry "Water now — the leaves are crisping up."; crispyMoist "Water now — crispy leaves even though the soil's damp."; crispyWet "A light water — crispy leaves but the soil's wet." (match each site's voice; finalise any placeholders unit B may have left). Leave the `leaves = .fine` defaults unchanged. Do NOT touch the engine, the Reason enum, or RecommendationPresentation (unit B).

## Done when
Both leaf-question labels read 'How do the leaves look?' (or the agreed CheckInView variant); both leaves pickers show three selectable segments in the order Crispy / Fine / Droopy; both view-model `message` switches handle the crispy reasons with site-appropriate copy; the project builds and the test suite passes; and a `./build_run.sh Sprout-Claude -seedDemoData YES` screenshot of the check-in (or guided-watering report) leaf picker, read back from `screenshots/latest.png`, visibly shows the new Crispy segment first, then Fine, then Droopy, with the reworded question above it.
