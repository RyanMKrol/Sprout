# T025 — Sequential photo capture restyle

## Overview
The multi-plant camera cover in the new dark language, keeping the existing capture/advance
machinery and feedback.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 13 and `Sources/Views/PhotoCaptureView.swift`
   + `Sources/ViewModels/PhotoCaptureCoordinator.swift` first — capture logic and coordinator
   stay as-is; this is a reskin.
2. Restyle: background #10160E. Header centered: plant name display(22) white; second line
   bodyItalic species + " · {i} of {n}" body(14) white@0.6. Viewfinder: square, margin 22,
   radius 26. Success feedback: 3pt #4FC07E border + rgba(63,126,88,0.5) overlay + white
   circleCheck 72pt pop-in (scale 0.5→1.1→1, 0.5s, keyed animation) + "Saved — next plant"
   display(18) white — replacing the old flash/green pulse visuals but firing on the same
   events/haptics. Bottom: "Skip" body(17) white left; shutter 84Ø white ring (4pt) + 70Ø fill.
3. Keep the `camera` deep link and identifiers.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN camera;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: dark screen,
  white display-font plant name + "of" counter, rounded square viewfinder, white shutter.
- PhotoCaptureCoordinator tests green.
