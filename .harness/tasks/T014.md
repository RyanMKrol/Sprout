# T014 — Launch splash screen (NEW)

## Overview
The new animated launch moment: green gradient, floating frosted logo tile, wordmark, spinner,
fading into Home shortly after launch.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 00.
2. Create `Sources/Views/LaunchSplashView.swift` per the mock: full-bleed
   `SproutTheme.launchGradient`; soft radial white glow (300Ø, white@0.08) upper-center;
   centered column — 114×114 radius-32 tile (white@0.12 fill, white@0.16 border, shadow
   0 22 50 black@0.32) containing `ChromeIcon.seedling` 58pt in SproutTheme.cream, gently
   floating (repeatForever translateY ±5, 3s ease-in-out, keyed `.animation(..., value:)` idiom);
   "Sprout" `SproutFont.display(36)` in #F4F1E7, 24 below the tile. 72 from the bottom: 26Ø
   spinner — Circle stroke 2.5 white@0.22 with a cream trimmed arc rotating 0.9s linear
   repeatForever (same keyed idiom).
3. In `Sources/SproutApp.swift`/`Sources/ContentView.swift`: overlay the splash over the root at
   launch and fade it out (~0.35s) 0.6s after appear (`.task { try? await Task.sleep(...) }`).
   Skip the delay entirely when `DemoSeed.isActive` so screenshots/tests never race it.
4. Set the static launch-screen background to #2F6B4C via the `UILaunchScreen` dict in
   `Sources/Info.plist` (background color asset) so the pre-SwiftUI frame matches.

## Done when
- App launches into the splash then fades to Home; with `-seedDemoData YES` the splash is
  skipped/instant.
- Run `./build_run.sh Sprout-Claude` (no seed), Read `screenshots/latest.png`: if capture lands
  post-fade that's fine — verify the splash instead by temporarily launching without seed and
  screenshotting within the first second, or confirm via the recorded state in code review; the
  committed screenshot must show either the splash (gradient + tile + wordmark) or Home with no
  visual regression.
- All existing tests stay green.
