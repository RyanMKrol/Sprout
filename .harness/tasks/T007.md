# T007 — Button styles + SectionEyebrow

## Overview
The three redesign button styles (primary green, ghost outline, cream-on-hero) and the
uppercase section label, as reusable styles.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §2.3 + §2.7(eyebrow) and the architecture
   contract (Buttons.swift).
2. Create `Sources/DesignSystem/Buttons.swift`:
   - `SproutPrimaryButtonStyle`: brandGreen fill, radius 16, vertical padding 16, white label
     `SproutFont.body(17,.semibold)`, shadow rgba(47,107,76,0.34) radius 13 y 6; pressed → scale 0.98.
   - `SproutGhostButtonStyle`: clear fill, 1.5pt stroke rgba(47,107,76,0.32), radius 18,
     padding 14, brandGreen label body(15,.semibold).
   - `SproutCreamButtonStyle`: #F4F1E7 fill, radius 15, padding 14, label #1C4330 body(16,.semibold).
3. Create `SectionEyebrow(_ text: String)` view in the same file: uppercase,
   `SproutFont.body(11,.bold)`, tracking 1.4, color SproutTheme.taupe.

## Done when
- Project builds; a `#Preview` shows all three buttons + an eyebrow.
- Style names/signatures match the architecture contract; no screens changed yet.
