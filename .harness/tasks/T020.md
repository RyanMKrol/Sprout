# T020 — Edit Plant form + delete confirmation

## Overview
The edit sheet in new chrome — photo row, icon row (opens the icon picker), nickname, room,
and the in-form Delete Plant row backed by the new confirm dialog.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screens 08/09 and
   `Sources/Views/PlantEditView.swift` + `Sources/ViewModels/PlantEditViewModel.swift` first.
   The icon picker sheet (`IconPickerView`, T021) already exists — present it, don't build it.
2. Restyle with `SproutSheetHeader` (Cancel / "Edit Plant" / Save) on paper. Sections by
   eyebrow: "PHOTO" — white row: 64Ø `PlantToken` (photo wins) + "Change photo" green body(17,
   .medium) → existing single-shot camera cover. "ICON" — white row: current glyph in a 34Ø
   token + "Change icon" green → present `IconPickerView` for this plant. "NICKNAME" — white
   field body(17). "ROOM" — white row with Menu picker (as today). No species field.
3. Bottom (30 gap): white radius-18 row, border rgba(196,85,59,0.2): trash icon + "Delete
   Plant" body(17,.semibold) #C4553B → `SproutAlert` "Delete {name}?" / "This removes the plant
   and its check-in history. This can't be undone." / Cancel / Delete(#C4553B). Confirm deletes
   via the existing view-model/repository path and dismisses to the list.
4. Keep the `edit` deep-link auto-open and identifiers.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN edit;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: new sheet
  chrome, PHOTO/ICON/NICKNAME/ROOM sections, red Delete Plant row.
- PlantEditViewModel tests green; delete flows through the confirm dialog.
