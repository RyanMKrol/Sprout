# T019 — Adjust-schedule sheet restyle

## Overview
The medium-detent wheel sheet that sets the next watering date, in the new sheet chrome.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 07; find the current sheet
   (ScheduleEditorSheet inside `Sources/Views/PlantDetailView.swift`).
2. Restyle using `SproutSheetHeader` (Cancel / "Adjust schedule" / Save) +
   `.sproutSheetBackground()`, `.presentationDetents([.medium])`. Label "Water this plant in"
   body(15,.semibold) #6E7A63 centered. Keep the existing 0–365 wheel (Picker .wheel) with rows
   "Today", "1 day", "2 days"… — restyle the selected look as closely as SwiftUI allows (the
   soft-green highlight band comes free from the wheel; set the row font display(20) via
   .pickerStyle contents). Footer hint verbatim: "Sets the next-watering date. Future check-ins
   keep adapting it." body(13) #8A9080 centered.
3. Behavior unchanged (same save semantics as today).

## Done when
- Screenshot via `SPROUT_SCREEN=detail` then tapping is not scriptable — instead verify by
  `#Preview` + code review, AND the detail screenshot still renders (no regression):
  `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN detail;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read screenshots/latest.png.
- Existing schedule-override behavior/tests unchanged and green.
