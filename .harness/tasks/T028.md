# T028 — Room detail screen (NEW) + roomdetail hook + environment copy

## Overview
The new per-room page: brightness/humidity stat cards, a plain-language watering-impact
banner, and the room's plants — closing the old "rooms feel dead-end" flag.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 15 (including the ⚑ derived copy
   formula) and `Sources/Engine/RoomEnvironment.swift` (the environment factor) +
   `Sources/ViewModels/RoomsViewModel.swift`.
2. Create a pure helper (in `Sources/Views/RoomDetailView.swift` or `Sources/Support/`):
   `static func impactLine(factor: Double) -> String` — factor > 1: "…dries soil faster —
   plants here are watered about {P}% more often." with P = round(|1−factor|×100), lead word
   from brightness ("Bright light" / "Low light" per the room's brightness); factor < 1:
   "…watered about {P}% less often."; factor == 1: "Balanced light and humidity — no adjustment
   to watering here." Add `Tests/RoomDetailTests.swift` pinning factor 1.15 → "15% more often",
   0.9 → "10% less often", 1.0 → the balanced line.
3. Create `Sources/Views/RoomDetailView.swift`: paper; back "‹ Rooms"; "Edit" text button →
   the existing room editor sheet. Title room name display(32). Stat duo: oat card (sun
   #D98B0A, "Brightness" body(12,.semibold) #8A7A55, value display(19)) + sage card (droplet
   green, "Humidity", value). `InfoBanner(arrowTrendUp, impactLine)`. "PLANTS IN THIS ROOM"
   eyebrow; compact white rows: `PlantToken` 40 + name + bodyItalic species + small `DueChip` →
   NavigationLink to plant detail. Empty: hint "No plants in this room yet."
4. Wire the `roomdetail` SPROUT_SCREEN case in HomeView's deep-link switch: push Rooms then
   the first room's detail.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN roomdetail;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: stat duo
  cards, green impact banner, plant rows with chips.
- `RoomDetailTests` pass verbatim; full suite green.
