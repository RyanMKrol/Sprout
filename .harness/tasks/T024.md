# T024 — Photo prompt restyle

## Overview
The "plants added — take photos?" sheet in the new language.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 12 and `Sources/Views/PhotoPromptView.swift`
   (+ its pure `PhotoPromptText` copy enum and tests).
2. Restyle: bottom sheet on paper; 104Ø logoGradient circle with cream camera 40pt; title
   display(27) from PhotoPromptText (update copy to the spec's "Three plants added 🌱" pattern —
   spell counts one…nine as words, capitalize first; singular "One plant added 🌱"); body(16)
   verbatim "Add a photo of each so they're easy to spot. You can always do this later.";
   `PrimaryButton` "Take Photos"; text button "Skip photos" green body(17,.semibold).
3. Update `Tests/PhotoPromptTextTests.swift` to the new copy (this is a legitimate copy change).

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN photoprompt;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: gradient
  camera circle, display title, green CTA.
- PhotoPromptText tests updated and green; behavior unchanged.
