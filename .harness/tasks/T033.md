# T033 — Guided watering: report + action screens restyle

## Overview
The per-plant guided flow (assess → recommendation → water/skip) in the new full-cover
language with progress bar, reusing RecommendationPresentation.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screens 20/21 and
   `Sources/Views/GuidedWateringView.swift` + `Sources/ViewModels/GuidedWateringCoordinator.swift`
   first — coordinator logic (modes, advance rules, "schedule only advances on a real water")
   is untouched.
2. Restyle the report step: full cover on paper; header "Close" body(17) green / "Water your
   plants" display(17) / "Skip" body(17) green. Progress: 5pt track #E1DBCB + brandGreen fill
   proportional to position/total, radius 3. Hero: `PlantToken` 120 + name display(22) +
   bodyItalic "{species} · {i} of {n}". "HOW DOES IT LOOK?" + Fine/Droopy and "HOW'S THE SOIL?"
   + Dry/Moist/Wet (`SproutSegmentedPicker`). Pinned `PrimaryButton` "Check".
3. Action step: same chrome/hero; recommendation card `.sproutCard()` radius 22 padding 26×22
   centered — 78Ø tinted circle + icon 34 + headline display(20) from
   `RecommendationPresentation`. Pinned `PrimaryButton` "💧 I watered it" + text button
   "Didn't water — next" green body(17,.semibold). Keep all coordinator semantics + the
   `water` deep link + identifiers.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN water;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: progress
  bar, 120 token, display name with "· 1 of N", segmented groups, pinned Check button.
- GuidedWateringCoordinator tests green.
