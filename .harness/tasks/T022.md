# T022 — Add flow: room step restyle

## Overview
Step 1 of adding plants ("Where do they live?") in the new sheet language.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 10 and `Sources/Views/AddFlowView.swift`
   (room-step section) + `Sources/ViewModels/BasketAddViewModel.swift` first.
2. Restyle the room step: full-height sheet on paper (`.sproutSheetBackground()`), handle,
   right-aligned "Cancel". Title display(28) "Where do they\nlive?"; body(14) #6E7A63 verbatim
   spec copy. "YOUR ROOMS" eyebrow; white radius-18 rows: 40×40 radius-12 rgba(180,131,47,0.14)
   house icon #B4832F + name display(17) + env summary body(12.5) + chevronRight — tap selects
   and advances (as today). `GhostButton` "+ Add a new room" → existing add-room sheet. Text
   button "Skip — no room for now" body(15,.medium) #8A9080 (existing skip behavior).
3. Keep staged-dismissal behavior and the `addflow` deep link; keep identifiers.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN addflow;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: new room
  step (display title, white room rows with oat house bubbles, ghost add-room button).
- BasketAddViewModel tests green.
