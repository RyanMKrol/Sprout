# T030 — Edit Room sheet restyle + delete row

## Overview
The room editor with segmented light controls, live brightness readout, and an in-form
Delete Room row (confirm via SproutAlert).

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screens 17/15b and the RoomEditorView
   section of `Sources/Views/RoomsView.swift`.
2. Restyle with `SproutSheetHeader` (Cancel / "Edit Room" / Save) on paper. Name field white
   r16. "DIRECT SUN ⓘ" + "INDIRECT SUN ⓘ": `SproutSegmentedPicker` Low/Medium/High with the
   existing `RoomInfoHeader` ⓘ popovers. Live readout white r16 card: sun icon + "Brightness"
   body(16) + amber chip with the inferred value (existing inference logic), helper verbatim:
   "Inferred from direct + indirect light. Brighter rooms dry out faster, so plants there are
   watered more often." "HUMIDITY": `SproutSegmentedPicker` Dry/Normal/Moist.
3. Bottom: white radius-18 row, border rgba(196,85,59,0.2): trash + "Delete Room" body(17,
   .semibold) #C4553B → the same `SproutAlert` copy as T027's swipe-delete → existing delete
   path, dismiss to the rooms list.
4. Keep the `roomeditor` deep link working end-to-end.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN roomeditor;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: segmented
  pickers with green pills, brightness readout chip, red Delete Room row.
- RoomsViewModel tests green.
