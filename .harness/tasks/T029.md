# T029 — Add Room sheet restyle (preset wheel + Other path)

## Overview
The add-room sheet: preset wheel with live typical settings, plus the custom "Other…" path
using the light/humidity controls inline.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 16 (incl. the ⚑ derived Other…
   behavior) and `Sources/Views/AddRoomView.swift` (current presets + Other already exist).
2. Restyle with `SproutSheetHeader` (Cancel / "Add Room" / Add) on paper. "ROOM TYPE" eyebrow;
   white card containing the preset wheel (existing preset list + "Other…" last), selected row
   display(20). "TYPICAL SETTINGS" eyebrow; white card: "Brightness" row → amber chip (#C4832A
   on rgba(217,139,10,0.14), radius 10) / divider / "Humidity" row → soft-green chip — values
   live-update from the selected preset (existing inference). Selecting "Other…" swaps the
   TYPICAL SETTINGS card for: name field (white r16) + the DIRECT SUN / INDIRECT SUN / HUMIDITY
   `SproutSegmentedPicker` groups with their ⓘ `RoomInfoHeader`s (reuse existing popover copy).
   Footer hint verbatim: "Pick a common room and Sprout fills in typical light and humidity —
   fine-tune it later by editing the room."
3. Keep the `addroom` deep link and save semantics.

## Done when
- `SIM=$(./tools/loop_sim.sh); xcrun simctl spawn $SIM launchctl setenv SPROUT_SCREEN addroom;
  ./build_run.sh Sprout-Claude -seedDemoData YES` → Read `screenshots/latest.png`: wheel in a
  white card, TYPICAL SETTINGS card with amber + green chips, footer hint.
- Existing room-preset tests (RoomPresetTests) green.
