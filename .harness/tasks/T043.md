## Overview
Re-attempt of T034 (the 'All done 🌿' completion screen of guided watering). The first attempt was blocked for scope-creep: it touched Sources/ContentView.swift and Sources/ViewModels/GuidedWateringCoordinator.swift, which the original scope (Sources/Views/** + Tests/**) and the 'Coordinator untouched' instruction forbade. But those edits are UNAVOIDABLE: the '## Done when' requires the screen to render BOTH mode bodies (due vs all-plants), yet the coordinator never stores its Mode — ContentView.makeGuidedWatering discards it after picking the plant set — so the view has no way to know the mode. The fix is to persist `mode` on the coordinator, pass it from ContentView, switch the completion copy on it, and cover it with a pure helper + unit test. Scope now includes the coordinator and ContentView so exactly that change can land.

## Do
1. Read `.harness/docs/designs/redesign-spec.md` §3 screen 22, `Sources/Views/GuidedWateringView.swift` (the `completionStep` computed var, currently hardcoded to the due-mode body), `Sources/ViewModels/GuidedWateringCoordinator.swift` (has `enum Mode { case due, all }` but stores no mode), and `Sources/ContentView.swift`'s `makeGuidedWatering(_ mode:)` (selects plants by mode, then discards the mode).
2. Thread the mode through so the view can read it: add a stored `let mode: Mode` (default `.all`) to `GuidedWateringCoordinator` and set it from the initializer; pass the `mode` argument through from `ContentView.makeGuidedWatering` into the `GuidedWateringCoordinator(...)` init. Do NOT change how the plant set is selected or any other coordinator behaviour — this is a pass-through of the existing `mode` value only.
3. Add a pure, testable helper for the completion body copy — a `static func completionBody(for mode: Mode) -> String` on `GuidedWateringCoordinator` (or an equivalent pure function) returning: `.due` → "You've been through every plant that needed water today."; `.all` → "You've checked in on every plant." Have `completionStep` render `coordinator's` mode through this helper instead of the hardcoded string.
4. Confirm/keep the rest of the completion restyle per screen 22: header shows only a "Close" button; centered 104Ø success circle (fill `PlantTokenPalette.success` duo) with a white check ~46pt and a pop-in on appear (scale 0.5→1.1→1 keyed `.animation(..., value:)` idiom); "All done 🌿" `SproutFont.display(27, weight: .bold)`; body `SproutFont.body(16)` in the muted green (`SproutTheme.textMuted` / #6E7A63); a pinned primary "Done" button using the project's `SproutPrimaryButtonStyle`. (Note: the current file uses `SproutTheme.brandGreen` for the circle and `body(15)` — align these to the spec's `PlantTokenPalette.success` and `body(16)` while you are here.)
5. Add/extend a unit test in `Tests/**` (e.g. `Tests/GuidedWateringCoordinatorTests.swift`) asserting BOTH exact body strings via the pure helper: `completionBody(for: .due)` and `completionBody(for: .all)`. Keep the `#Preview` rendering both mode bodies (two coordinators, one per mode).

## Done when
`GuidedWateringCoordinator` stores its `Mode` and exposes a pure `completionBody(for:)` helper; `ContentView.makeGuidedWatering` passes the mode into the init; `completionStep` renders the mode-appropriate body via the helper (no hardcoded single string). A unit test asserts both exact strings — `.due` → "You've been through every plant that needed water today." and `.all` → "You've checked in on every plant." — and passes. The completion screen matches screen 22 (Close-only header, 104Ø success-duo circle + white check with pop-in, display(27) title, body(16) muted-green, pinned primary Done). No edits land outside the declared scope.
